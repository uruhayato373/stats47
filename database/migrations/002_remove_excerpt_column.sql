-- excerptカラムの削除
-- 作成日: 2025-01-31
-- 説明: articlesテーブルからexcerptカラムを削除し、descriptionに統一

-- ============================================================================
-- excerptカラムの削除
-- ============================================================================

-- SQLiteではALTER TABLE DROP COLUMNが直接サポートされていないため、
-- 新しいテーブルを作成してデータを移行する方法を使用
-- ただし、D1ではALTER TABLE DROP COLUMNがサポートされている可能性があるので、
-- まずは直接削除を試みる

-- D1/CloudflareではALTER TABLE DROP COLUMNがサポートされている場合
-- このコマンドが失敗した場合は、テーブル再作成が必要

-- 注意: 本番環境ではデータのバックアップを取ってから実行してください

-- excerptカラムを削除
-- 注意: SQLite/D1ではALTER TABLE DROP COLUMNがサポートされている場合のみ動作します
-- 失敗する場合は、テーブル再作成が必要です（データのバックアップが必要）

-- まず、excerptカラムが存在するか確認し、存在する場合のみ削除
-- SQLiteでは直接DROP COLUMNできないため、テーブル再作成が必要な場合があります
-- ただし、D1（SQLite 3.35.0+）ではDROP COLUMNがサポートされています

-- excerptカラムを削除
-- 注意: このコマンドが失敗する場合は、以下のコメントアウトされた方法を使用してください
ALTER TABLE articles DROP COLUMN excerpt;

-- 代替方法（テーブル再作成）:
-- 1. 新しいテーブルを作成（excerptカラムなし）
-- 2. データを移行
-- 3. 古いテーブルを削除
-- 4. 新しいテーブルをリネーム
-- ただし、これはデータが失われる可能性があるため、慎重に実行してください

