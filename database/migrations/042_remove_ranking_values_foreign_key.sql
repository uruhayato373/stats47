-- ============================================================================
-- マイグレーション: ranking_valuesテーブルの外部キー制約を削除
-- ============================================================================
-- 作成日: 2025-01-31
-- 説明: ranking_valuesテーブルがranking_itemsを参照する外部キー制約が
--       古い構造（ranking_keyのみ）のため、新しい複合主キーと一致しない
--       このテーブルは使用しないため、外部キー制約を削除するかテーブル自体を削除

-- ============================================================================
-- 1. ranking_valuesテーブルが存在する場合、外部キー制約を削除するために再作成
-- ============================================================================

-- テーブルが存在する場合は、外部キー制約なしで再作成
-- データは保持しない（使用しないテーブルのため）

DROP TABLE IF EXISTS ranking_values;

-- 注意: ranking_valuesテーブルは使用しません（設計により R2 ストレージを使用）
-- ランキング値データは R2 Storage に JSON 形式で保存されます
-- パス: ranking/{areaType}/{rankingKey}/{timeCode}.json

