# コンポーネントディレクトリ構造整理

## 概要

`meta-info`と`stats-data`のコンポーネントを`MainContents`と`Sidebar`ディレクトリに分類し、役割を明確化する。

## 変更前後の構造

### meta-info

**変更前:**

```
meta-info/components/
├── EstatMetaInfoDisplay/
├── EstatMetaInfoFetcher/
├── EstatMetaInfoSidebar/
├── SavedMetaInfoListItem/
└── index.ts
```

**変更後:**

```
meta-info/components/
├── MainContents/
│   └── EstatMetaInfoDisplay/
│       ├── EstatMetaInfoDisplay.tsx
│       ├── EstatMetaInfoDisplay.stories.tsx
│       ├── index.ts
│       └── tabs/
│           ├── AreasTab.tsx
│           ├── CategoriesTab.tsx
│           ├── TableInfoTab.tsx
│           └── TimeAxisTab.tsx
├── Sidebar/
│   ├── EstatMetaInfoFetcher/
│   │   ├── EstatMetaInfoFetcher.tsx
│   │   ├── EstatMetaInfoFetcher.stories.tsx
│   │   └── index.ts
│   ├── EstatMetaInfoSidebar/
│   │   ├── EstatMetaInfoSidebar.tsx
│   │   ├── EstatMetaInfoSidebar.stories.tsx
│   │   └── index.ts
│   └── SavedMetaInfoListItem/
│       ├── SavedMetaInfoListItem.tsx
│       ├── SavedMetaInfoListItem.stories.tsx
│       └── index.ts
└── index.ts
```

### stats-data

**変更前:**

```
stats-data/components/
├── EstatDataDisplay/
│   └── components/
└── EstatDataFetcher/
└── index.ts
```

**変更後:**

```
stats-data/components/
├── MainContents/
│   └── EstatDataDisplay/
│       ├── EstatDataDisplay.tsx
│       ├── index.ts
│       └── components/
│           ├── EstatAreasTable.tsx
│           ├── EstatCategoriesTable.tsx
│           ├── EstatOverview.tsx
│           ├── EstatRawData.tsx
│           ├── EstatValuesTable.tsx
│           └── EstatYearsTable.tsx
├── Sidebar/
│   └── EstatDataFetcher/
│       ├── EstatDataFetcher.tsx
│       └── index.ts
└── index.ts
```

## 実装手順

### Phase 1: meta-info の整理

#### 1-1. 新しいディレクトリ作成

```bash
mkdir src/features/estat-api/meta-info/components/MainContents
mkdir src/features/estat-api/meta-info/components/Sidebar
```

#### 1-2. MainContents への移動

```bash
# EstatMetaInfoDisplay を MainContents へ移動
mv src/features/estat-api/meta-info/components/EstatMetaInfoDisplay \
   src/features/estat-api/meta-info/components/MainContents/
```

#### 1-3. Sidebar への移動

```bash
# 各サイドバーコンポーネントを Sidebar へ移動
mv src/features/estat-api/meta-info/components/EstatMetaInfoFetcher \
   src/features/estat-api/meta-info/components/Sidebar/

mv src/features/estat-api/meta-info/components/EstatMetaInfoSidebar \
   src/features/estat-api/meta-info/components/Sidebar/

mv src/features/estat-api/meta-info/components/SavedMetaInfoListItem \
   src/features/estat-api/meta-info/components/Sidebar/
```

#### 1-4. index.ts の更新

**`src/features/estat-api/meta-info/components/index.ts`**

```tsx
// MainContents
export { EstatMetaInfoDisplay } from "./MainContents/EstatMetaInfoDisplay";

// Sidebar
export { EstatMetaInfoFetcher } from "./Sidebar/EstatMetaInfoFetcher";
export { EstatMetaInfoSidebar } from "./Sidebar/EstatMetaInfoSidebar";
```

#### 1-5. インポートパスの修正

**影響を受けるファイル:**

1. `src/app/admin/dev-tools/estat-api/@main/meta-info/page.tsx`

```tsx
// 変更前
import { EstatMetaInfoDisplay } from "@/features/estat-api/meta-info/components";

// 変更後（変更なし、index.tsで吸収）
import { EstatMetaInfoDisplay } from "@/features/estat-api/meta-info/components";
```

2. `src/app/admin/dev-tools/estat-api/@sidebar/meta-info/page.tsx`

```tsx
// 変更前
import { EstatMetaInfoSidebar } from "@/features/estat-api/meta-info/components";

// 変更後（変更なし、index.tsで吸収）
import { EstatMetaInfoSidebar } from "@/features/estat-api/meta-info/components";
```

3. `src/features/estat-api/meta-info/components/Sidebar/EstatMetaInfoSidebar/EstatMetaInfoSidebar.tsx`

```tsx
// 変更前
import { EstatMetaInfoFetcher } from "../EstatMetaInfoFetcher";
import { SavedMetaInfoListItem } from "../SavedMetaInfoListItem";

// 変更後
import { EstatMetaInfoFetcher } from "../EstatMetaInfoFetcher";
import { SavedMetaInfoListItem } from "../SavedMetaInfoListItem";
// ※相対パスは同じ（同じSidebarディレクトリ内）
```

### Phase 2: stats-data の整理

#### 2-1. 新しいディレクトリ作成

```bash
mkdir src/features/estat-api/stats-data/components/MainContents
mkdir src/features/estat-api/stats-data/components/Sidebar
```

#### 2-2. MainContents への移動

```bash
mv src/features/estat-api/stats-data/components/EstatDataDisplay \
   src/features/estat-api/stats-data/components/MainContents/
```

#### 2-3. Sidebar への移動

```bash
mv src/features/estat-api/stats-data/components/EstatDataFetcher \
   src/features/estat-api/stats-data/components/Sidebar/
```

#### 2-4. index.ts の更新

**`src/features/estat-api/stats-data/components/index.ts`**

```tsx
// MainContents
export { default as EstatDataDisplay } from "./MainContents/EstatDataDisplay";

// Sidebar
export { default as EstatDataFetcher } from "./Sidebar/EstatDataFetcher";
```

#### 2-5. インポートパスの修正

**影響を受けるファイル:**

1. `src/app/admin/dev-tools/estat-api/@main/stats-data/page.tsx`

```tsx
// 変更なし（index.tsで吸収）
import { EstatDataDisplay } from "@/features/estat-api/stats-data/components";
```

2. `src/app/admin/dev-tools/estat-api/@sidebar/stats-data/page.tsx`

```tsx
// 変更なし（index.tsで吸収）
import { EstatDataFetcher } from "@/features/estat-api/stats-data/components";
```

## 実行コマンド（Git 操作）

```bash
# meta-info
git mv src/features/estat-api/meta-info/components/EstatMetaInfoDisplay \
       src/features/estat-api/meta-info/components/MainContents/

git mv src/features/estat-api/meta-info/components/EstatMetaInfoFetcher \
       src/features/estat-api/meta-info/components/Sidebar/

git mv src/features/estat-api/meta-info/components/EstatMetaInfoSidebar \
       src/features/estat-api/meta-info/components/Sidebar/

git mv src/features/estat-api/meta-info/components/SavedMetaInfoListItem \
       src/features/estat-api/meta-info/components/Sidebar/

# stats-data
git mv src/features/estat-api/stats-data/components/EstatDataDisplay \
       src/features/estat-api/stats-data/components/MainContents/

git mv src/features/estat-api/stats-data/components/EstatDataFetcher \
       src/features/estat-api/stats-data/components/Sidebar/
```

## メリット

1. **役割の明確化**: MainContents と Sidebar で用途が一目瞭然
2. **保守性向上**: 関連コンポーネントをグループ化
3. **スケーラビリティ**: 新規コンポーネントの配置先が明確
4. **一貫性**: meta-info と stats-data で統一された構造
5. **検索性**: ディレクトリ名で役割を絞り込める

## 注意事項

1. **Git 操作**: `git mv`を使用して履歴を保持
2. **インポートパス**: `index.ts`で外部インポートパスは変更なし
3. **内部インポート**: 同じディレクトリ内の相対パスは基本的に変更なし
4. **Storybook**: `.stories.tsx`ファイルも一緒に移動
5. **テスト**: テストファイルがある場合も同様に移動

## 影響を受けるファイル（最小限）

- `src/features/estat-api/meta-info/components/index.ts`
- `src/features/estat-api/stats-data/components/index.ts`
- 内部の相対インポートパス（必要に応じて）

外部からのインポートは`index.ts`の re-export で吸収するため、変更不要。
