---
slug: information-communication-coefficient-ranking
reviewer: blog-critic
mode: delta
verdict: PASS
date: 2026-09-06
---

## 評価サマリ

前回唯一残っていた blocker（時系列図の中に「全国平均」呼びが残り本文と矛盾していた件）が解消したことを実測で確認しました。`info-comm-coefficient-national-trend.json` の `title` は「情報通信係数の47都道府県庁所在市平均の推移」、`label` は「47都道府県平均」に修正され、SVG も再生成されています。記事ディレクトリ全体（`article.md` / `data/*.json` / `data/*.svg`）を grep したところ「全国平均」の文字列は 1 件も残っていません。家計調査の都道府県別データの基準は 47 県庁所在市の単純平均であって総務省が公表する全国集計値ではないため、この表記に直ったことで図・本文・`source.json` の `transform: "47都道府県の単純算術平均を年ごとに算出"` が三者とも同じことを言う状態になりました。SVG が本文から切り離されて SNS や検索結果に流通しても、図単体で出典を誤らせません。

## 指摘

- [解消] **時系列図の「全国平均」呼び**（前回 BLOCK）。SVG の描画テキストを抽出すると、図タイトルが「情報通信係数の47都道府県庁所在市平均の推移」、凡例が「47都道府県平均」、画像下部のキャプションが「情報通信係数の47都道府県庁所在市平均の推移（％）」に変わっていました。テキストの差し替えだけでなくデータからの再生成が実際に行われたことも確認済みで、polyline の点数は 18 点で JSON と一致し、最も上に位置する点が index 13（2020年 5.40%）、最も下が index 17（2024年 4.18%）と JSON の最大値・最小値の位置に対応しています。手作業でテキストだけ書き換えた成果物ではありません。
- [MINOR] **図タイトルと凡例で母集団の書き方が揃っていない**（新規・公開を止めるものではない）。同じ図の中でタイトルは「47都道府県庁所在市平均」、凡例は「47都道府県平均」と、凡例側だけ「庁所在市」が落ちています。単系列チャートの凡例は線の識別が役割であり、同じ図のタイトルと直前の本文（「この平均は総務省が公表する全国集計値ではなく、47都道府県の値を単純に算術平均したものです」）が母集団を明示しているため誤読は生じません。次回触るときに凡例も揃えられれば理想的です。
- [MINOR] **地方で実額が高い理由の掘り下げが仮説の列挙にとどまる**（前回からの継続）。固定電話の残存・ケーブルテレビ受信料・NHK受信料の世帯単位課金を `[仮説]` として提示し、品目別内訳がないという限界も明示しているため捏造でも断定でもありません。ただし 5 品目のうちどれが効いているかは特定できておらず、タイトルの問いへの回答は仮説どまりです。次回 brushup で費目別データを接地できれば記事の核が完成します。
- [MINOR] **指標の出自が依然として無出典**（前回からの継続）。無名の権威づけは削除済みですが、情報通信係数を誰が提案した指標なのかは示されていません。

## 判定理由

未解消の blocker が無くなったため `verdict: PASS` とします。決定的 gate（`quality-gate.mjs`）は critic 未通過の 1 件を除いて blocker 0・warning 0 で、callout 4・内部リンク 6 種でリンク切れ 0・図あたり prose 1,011 字・括弧内数値 0・markdown 表 0・である調 0・SVG 欠落 0・無意味名 0・系譜欠落 0・構造エラー 0・寸法違反 0・json ペア違反 0 でした。変更 hunk 限定の regression スポットチェックとして、同じタイミングで再生成された他 4 枚の SVG も描画テキストを抽出して本文と突合し、情報通信係数ランキング（1位 秋田県 5.4％）・情報通信関係費実額（1位 島根県 179,879円 / 2位 秋田県 177,295円）・消費支出総額（1位 埼玉県 4,294,300円 / 2位 東京都 4,211,608円）・タイルマップ（2024年・1位 秋田県 5.4％）のいずれも本文の記述と一致し、今回の再生成で失われた情報はありませんでした。残る 3 点はいずれも MINOR で、公開を止める理由にはなりません。
