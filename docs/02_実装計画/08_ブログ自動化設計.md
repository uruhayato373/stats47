---
type: implementation-plan
date: 2026-06-02
updated: 2026-06-13
status: active
tags: [blog, automation, github-actions, dbless]
---

# ブログ記事リライト 自動化設計

GSC 高インプレ記事を「候補選定 → データ接地 → AI リライト → 品質ゲート → 公開 → 効果計測」まで
クラウドで半自律実行する環境の設計。2026-06-02 に quality-gate を CI enforce 化したことで、
薄い/捏造記事を自動公開する事故を構造的に防げるようになり、自律化が安全に可能になった。

## なぜ「2 基盤分割」がベストか

クラウド実行基盤は能力が異なる。**AI を動かせる基盤と R2 を書ける基盤が別**なので、役割分担する。

| 役割 | 基盤 | 可能 | 不可能 |
|---|---|---|---|
| AI リライト → PR | **Pro セッション (手動 or Routine)** | LLM 推論 / Edit / script 実行 / `gh pr create` / develop push | R2 書き込み / workflow dispatch (403) |
| PR auto-merge | GitHub Actions | ネイティブ権限で merge | — |
| **develop → R2 公開** | **GitHub Actions** (`blog-auto-publish.yml`) | R2 secrets 保持 (push 自動発火 = dispatch 不要) | LLM 実行 |
| 効果計測 | GitHub Actions (`fetch-metrics-weekly.yml`) | GSC snapshot 突合 | — |

## コンポーネント

| 工程 | 実装 | 状態 |
|---|---|---|
| ① 候補選定 (GSC) | `.claude/scripts/blog/select-brushup-candidates.mjs` | ✅ 既存 (CTR 改善余地ベース・GSC 必須) |
| ①' 候補選定 (catch-up) | `.claude/scripts/blog/select-conformance-candidates.mjs` | ✅ 2026-06-02・GSC 非依存・audit blocker ベース |
| ② データ接地 | `.claude/scripts/blog/fetch-ranking-data-r2.mjs` | ✅ R2 直接取得 (旧 D1 依存の置換) |
| ③ AI リライト | `/brushup-blog --target article <slug>` | ✅ 既存スキル |
| ④ チャート | `.claude/scripts/blog/generate-article-charts.mjs` | ✅ 上位5+下位5 / dark mode / --extract-inline |
| ⑤ critic (必須) | `blog-critic` agent → `review.md` (verdict PASS) | ✅ agent あり |
| ⑥ ゲート | `.claude/scripts/blog/quality-gate.mjs` (+factual) | ✅ CI enforce 済 + review.md PASS 必須 |
| ⑦ PR | Pro セッションが PR 作成 `--label brushup-auto` | ✅ trend pipeline と同型 |
| ⑧ auto-merge | `.github/workflows/blog-auto-merge.yml` | ✅ brushup-auto green で squash merge (未使用・brushup-auto PR 未作成) |
| ⑨ 公開ブリッジ | `.github/workflows/blog-auto-publish.yml` | ✅ **`on: push: [develop]` 自動発火 (2026-06-02 pilot 実証)** MAX_PUBLISH=10 |
| ⑩ 計測 | `.claude/scripts/blog/measure-gsc-impact.mjs` | ✅ `fetch-metrics-weekly.yml` 週次 cron に配線済 |

## 安全設計 (自動公開の肝)

1. **多重ゲート (ブロッキング)**: factual(数値捏造) + quality(構造/薄さ/rank 突合)。PR の CI と公開ブリッジの**両方**で再検証。捏造数値・薄い記事は R2 に到達不能。
2. **データ接地の徹底**: `data/*.json` を R2 観測値から決定的生成。LLM が digest 外の順位/値を書けば factual で落ちる。
3. **キルスイッチ**: 公開ブリッジは `on: push: [develop]` 自動発火。緊急停止は GitHub Actions UI の "Disable workflow"。
4. **量の上限**: 公開ブリッジ 1 実行 ≤10 本 (`MAX_PUBLISH=10`)。爆発半径を限定。
5. **クールダウン**: 同一 slug は効果計測まで N 週間 再改修しない (`auto-brushup-history.json`)。
6. **NotebookLM 禁止**: headless 非対応のため強制 `CTR-reframe`。
7. **ロールバック**: R2 の旧 `article.md` / `all.json` を上書き push で復旧。
8. **監査証跡**: 全変更が PR + wave 記録 + 改善ログに残る。

## 残作業

- [ ] **Claude Routine 登録** — Pro セッション手動運用で代替中。Routine 登録で日次自動化が可能になるが未着手。

## 関連

- 親方針: `docs/02_実装計画/01_収益化マスタープラン.md` / `docs/02_実装計画/03_改善バックログ.md`
- 品質基準 (正典): `.claude/rules/blog-quality-standards.md`
- wave 命名 / 効果計測: `.claude/rules/blog-data-schema.md`
- 完全DBレス: `docs/01_技術設計/12_完全DBレス設計.md`
- 自動化インベントリ: `docs/01_技術設計/06_自動化インベントリ.md`
