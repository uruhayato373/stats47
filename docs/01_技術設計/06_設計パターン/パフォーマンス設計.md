---
title: パフォーマンス設計
created: 2025-01-20
updated: 2025-01-20
tags:
  - stats47
  - performance
  - optimization
---

# パフォーマンス設計

## 概要

stats47 プロジェクトにおけるパフォーマンス最適化の設計戦略について説明します。

## パフォーマンス目標

### Core Web Vitals

| 指標                               | 目標値   | 測定方法               |
| ---------------------------------- | -------- | ---------------------- |
| **LCP** (Largest Contentful Paint) | < 2.5 秒 | 最大コンテンツ描画時間 |
| **FID** (First Input Delay)        | < 100ms  | 初回入力遅延           |
| **CLS** (Cumulative Layout Shift)  | < 0.1    | 累積レイアウトシフト   |

### その他の指標

- **TTFB** (Time to First Byte): < 600ms
- **FCP** (First Contentful Paint): < 1.8 秒
- **ページ読み込み時間**: < 3 秒
- **API レスポンス時間**: < 500ms

## フロントエンド最適化

### 1. バンドル最適化

#### Code Splitting

```typescript
// 動的インポートによる分割
const HeavyChart = dynamic(() => import("./HeavyChart"), {
  loading: () => <ChartSkeleton />,
  ssr: false,
});

// ルートベースの分割
const AdminPage = dynamic(() => import("./AdminPage"), {
  loading: () => <PageSkeleton />,
});
```

#### Tree Shaking

```typescript
// 必要な部分のみインポート
import { debounce } from "lodash/debounce";
import { format } from "date-fns/format";

// バレルエクスポートの回避
import { Button } from "@/components/Button";
import { Input } from "@/components/Input";
```

### 2. 画像最適化

#### Next.js Image コンポーネント

```typescript
import Image from "next/image";

export function OptimizedImage({ src, alt }: { src: string; alt: string }) {
  return (
    <Image
      src={src}
      alt={alt}
      width={800}
      height={600}
      priority={false} // 重要でない画像は遅延読み込み
      placeholder="blur"
      blurDataURL="data:image/jpeg;base64,..."
    />
  );
}
```

#### WebP フォーマット対応

```typescript
// 画像フォーマットの自動選択
export function ResponsiveImage({ src, alt }: { src: string; alt: string }) {
  return (
    <picture>
      <source srcSet={`${src}.webp`} type="image/webp" />
      <source srcSet={`${src}.jpg`} type="image/jpeg" />
      <img src={`${src}.jpg`} alt={alt} />
    </picture>
  );
}
```

### 3. コンポーネント最適化

#### React.memo の活用

```typescript
import { memo } from "react";

export const ExpensiveComponent = memo(
  ({ data }: { data: any }) => {
    return <div>{/* 重い処理 */}</div>;
  },
  (prevProps, nextProps) => {
    // カスタム比較関数
    return prevProps.data.id === nextProps.data.id;
  }
);
```

#### useMemo と useCallback

```typescript
import { useMemo, useCallback } from "react";

export function DataComponent({ data }: { data: any[] }) {
  // 重い計算のメモ化
  const processedData = useMemo(() => {
    return data.map((item) => expensiveCalculation(item));
  }, [data]);

  // コールバック関数のメモ化
  const handleClick = useCallback((id: string) => {
    // クリック処理
  }, []);

  return (
    <div>
      {processedData.map((item) => (
        <Item key={item.id} data={item} onClick={handleClick} />
      ))}
    </div>
  );
}
```

### 4. 状態管理最適化

#### Jotai の最適化

```typescript
import { atom } from "jotai";

// アトムの分割
export const userAtom = atom<User | null>(null);
export const userPreferencesAtom = atom<UserPreferences | null>(null);

// 派生アトムの最適化
export const userDisplayNameAtom = atom((get) => {
  const user = get(userAtom);
  return user ? `${user.firstName} ${user.lastName}` : "Guest";
});
```

## バックエンド最適化

### 1. データベース最適化

#### クエリ最適化

```sql
-- インデックスの活用（ranking_itemsの例）
CREATE INDEX idx_ranking_items_ranking_key ON ranking_items(ranking_key);
CREATE INDEX idx_ranking_items_is_active ON ranking_items(is_active);

-- 複合インデックス
CREATE INDEX idx_ranking_items_key_active ON ranking_items(ranking_key, is_active);
```

#### 接続プール

```typescript
// src/infrastructure/database/pool.ts
export class ConnectionPool {
  private static instance: ConnectionPool;
  private connections: D1Database[] = [];
  private maxConnections = 10;

  async getConnection(): Promise<D1Database> {
    if (this.connections.length > 0) {
      return this.connections.pop()!;
    }

    return getD1Database();
  }

  releaseConnection(connection: D1Database): void {
    if (this.connections.length < this.maxConnections) {
      this.connections.push(connection);
    }
  }
}
```

### 2. API 最適化

#### レスポンスキャッシング

```typescript
// src/app/api/data/route.ts
export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const cacheKey = `data-${searchParams.toString()}`;

  // キャッシュチェック
  const cached = await getCachedData(cacheKey);
  if (cached) {
    return Response.json(cached, {
      headers: {
        "Cache-Control": "public, max-age=3600",
        "X-Cache": "HIT",
      },
    });
  }

  // データ取得
  const data = await fetchData(searchParams);

  // キャッシュ保存
  await setCachedData(cacheKey, data, 3600);

  return Response.json(data, {
    headers: {
      "Cache-Control": "public, max-age=3600",
      "X-Cache": "MISS",
    },
  });
}
```

#### データ圧縮

```typescript
// src/infrastructure/compression.ts
import { gzip } from "zlib";
import { promisify } from "util";

const gzipAsync = promisify(gzip);

export async function compressResponse(data: any): Promise<Buffer> {
  const jsonString = JSON.stringify(data);
  return await gzipAsync(jsonString);
}
```

## キャッシング戦略

### 1. ブラウザキャッシング

#### HTTP キャッシュヘッダー

```typescript
// src/app/api/static-data/route.ts
export async function GET() {
  const data = await getStaticData();

  return Response.json(data, {
    headers: {
      "Cache-Control": "public, max-age=86400", // 24時間
      ETag: generateETag(data),
      "Last-Modified": new Date().toUTCString(),
    },
  });
}
```

#### Service Worker

```typescript
// public/sw.js
const CACHE_NAME = "stats47-v1";
const urlsToCache = ["/", "/static/css/main.css", "/static/js/main.js"];

self.addEventListener("install", (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(urlsToCache))
  );
});

self.addEventListener("fetch", (event) => {
  event.respondWith(
    caches.match(event.request).then((response) => {
      if (response) {
        return response;
      }
      return fetch(event.request);
    })
  );
});
```

### 2. CDN キャッシング

#### Cloudflare 設定

```typescript
// next.config.ts
export default {
  async headers() {
    return [
      {
        source: "/api/static/:path*",
        headers: [
          {
            key: "Cache-Control",
            value: "public, max-age=31536000, immutable",
          },
        ],
      },
    ];
  },
};
```

## 監視・分析

### 1. パフォーマンス監視

#### Web Vitals 測定

```typescript
// src/infrastructure/performance.ts
import { getCLS, getFID, getFCP, getLCP, getTTFB } from "web-vitals";

export function measureWebVitals() {
  getCLS((metric) => {
    console.log("CLS:", metric.value);
    sendMetric("CLS", metric.value);
  });

  getFID((metric) => {
    console.log("FID:", metric.value);
    sendMetric("FID", metric.value);
  });

  getFCP((metric) => {
    console.log("FCP:", metric.value);
    sendMetric("FCP", metric.value);
  });

  getLCP((metric) => {
    console.log("LCP:", metric.value);
    sendMetric("LCP", metric.value);
  });

  getTTFB((metric) => {
    console.log("TTFB:", metric.value);
    sendMetric("TTFB", metric.value);
  });
}

function sendMetric(name: string, value: number) {
  fetch("/api/metrics", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ name, value, timestamp: Date.now() }),
  });
}
```

#### パフォーマンス API

```typescript
// src/infrastructure/performance-api.ts
export function measurePerformance() {
  const navigation = performance.getEntriesByType(
    "navigation"
  )[0] as PerformanceNavigationTiming;

  const metrics = {
    domContentLoaded:
      navigation.domContentLoadedEventEnd -
      navigation.domContentLoadedEventStart,
    loadComplete: navigation.loadEventEnd - navigation.loadEventStart,
    totalTime: navigation.loadEventEnd - navigation.fetchStart,
  };

  console.log("Performance Metrics:", metrics);
  return metrics;
}
```

### 2. リアルタイム監視

#### パフォーマンスダッシュボード

```typescript
// src/app/admin/performance/page.tsx
export default function PerformancePage() {
  const [metrics, setMetrics] = useState<PerformanceMetrics[]>([]);

  useEffect(() => {
    const interval = setInterval(async () => {
      const data = await fetchPerformanceMetrics();
      setMetrics(data);
    }, 5000);

    return () => clearInterval(interval);
  }, []);

  return (
    <div>
      <h1>パフォーマンス監視</h1>
      <MetricsChart data={metrics} />
    </div>
  );
}
```

## 最適化チェックリスト

### フロントエンド

- [ ] Code Splitting の実装
- [ ] 画像の最適化（WebP、遅延読み込み）
- [ ] コンポーネントのメモ化
- [ ] バンドルサイズの監視
- [ ] Core Web Vitals の測定

### バックエンド

- [ ] データベースクエリの最適化
- [ ] API レスポンスのキャッシング
- [ ] 接続プールの実装
- [ ] データ圧縮の実装

### インフラ

- [ ] CDN の設定
- [ ] キャッシュヘッダーの設定
- [ ] Service Worker の実装
- [ ] 監視・アラートの設定

## 関連ドキュメント

- [システムアーキテクチャ](../01_システム概要/システムアーキテクチャ.md)
- [レンダリング戦略](../05_フロントエンド設計/レンダリング戦略.md)
- [データフェッチ設計](../04_データ設計/データフェッチ設計.md)
