# 自動化インベントリ

stats47 の全自動化タスクの静的な真実源。**新規追加・削除時は PR でこのファイルを更新**すること。

## いつ読むか
- どの計測・処理が今動いているか確認したいとき
- 新しい cron を追加する前に既存との重複チェック
- 壊れた自動化を特定するとき（docs を見て `launchctl list` / `gh workflow list` / `RemoteTrigger list` と照合）

## 運用ルール

- **追加時**: PR で本 docs に 1 行追加 + 変更理由を Issue で記録
- **壊れた時**: Issue 起票（label: `automation-broken`）、直ったら close、docs 変更不要（状態は Issue 側）
- **削除時**: PR で docs 行削除 + 関連 Issue に "deprecated" コメント
- **月 1 健康診断**: 月初に 3 基盤と docs が一致しているか確認

## 全実行基盤の概観

| 基盤 | 用途 | 前提 | 停止方法 |
|---|---|---|---|
| GitHub Actions | 公開 / 冪等な処理（PSI / GSC / GA4 / AdSense / Cloudflare cost / Security） | PC 非依存 | workflow 削除 or disable |
| launchd (ローカル Mac) | ブラウザセッション / ローカル資源必須 (D1 / Chrome profile) | Mac 起動必要 | `launchctl unload` |
| Claude Routine (Anthropic remote) | AI エージェント実行（/weekly-review など）、人間判断を自動化 | ANTHROPIC API billing、`.claude/state/triggers.json` | `RemoteTrigger` で `enabled: false` |

---

## GitHub Actions

### 計測・レポート系（schedule trigger）

| workflow | cron (UTC) | 日本時間 | 目的 | 出力先 | ステータス | 関連 |
|---|---|---|---|---|---|---|
| `psi-audit-daily.yml` | `0 17 * * *` | 毎日 02:00 JST | PSI 19 URL × mobile+desktop 計測、閾値違反で `[PSI Alert]` Issue 起票 | `.claude/state/metrics/psi/` → develop commit | 運用中 | #74 |
| `cloudflare-usage-daily.yml` | `30 17 * * *` | 毎日 02:30 JST | Cloudflare GraphQL から R2/Workers/D1 使用量を計測、閾値超過で `[Cloudflare Alert]` Issue 起票 | `.claude/state/metrics/cloudflare/` → develop commit | 運用中 | Phase 9 cost |
| `gsc-url-inspection-daily.yml` | `0 21 * * *` | 毎日 06:00 JST | URL Inspection API で 1,500 URL の Google 認識状態を取得、Coverage Drilldown 集計 | `.claude/state/metrics/gsc/url-inspection/` + `coverage-drilldown/` → main commit + #115 にコメント | 運用中 | #115 |
| `youtube-audit-daily.yml` | `0 0 * * *` | 毎日 09:00 JST | YouTube シャドウバン診断 + pause expiry / recovery judgment チェック | `.claude/state/metrics/youtube/` + `.claude/state/youtube-pause.json` → develop commit | 運用中 | `docs/10_SNS戦略/06_YouTube運用Playbook.md` |
| `fetch-metrics-weekly.yml` | `0 11 * * 0` | 日曜 20:00 JST | GSC/GA4/AdSense 週次 snapshot + NSM 週次 snapshot 生成 | `.claude/skills/analytics/{gsc,ga4,adsense}-improvement/reference/snapshots/YYYY-Www/` + `.claude/skills/management/nsm-experiment/reference/weekly-snapshots/YYYY-Www.json` → develop commit | 運用中 | #67 |
| `sns-metrics-weekly.yml` | `30 11 * * 0` | 日曜 20:30 JST | Instagram + YouTube メトリクス集計 | `.claude/skills/analytics/sns-metrics-improvement/snapshots/YYYY-MM-DD/metrics.csv` → develop commit | 運用中 | #89 |
| `sns-weekly-report.yml` | `0 12 * * 0` | 日曜 21:00 JST | SNS 週次レポート生成（data 部のみ、分析は週次レビュー Routine が追記） | `docs/04_レビュー/sns-weekly-report/YYYY-Www.md` → develop commit | 運用中 | #89 |
| `youtube-weekly-review.yml` | `15 0 * * 1` | 月曜 09:15 JST | 直近 7 日 vs 前 7 日サマリで `LATEST.md` 更新、開いている Recovery Issue にコメント | `.claude/state/metrics/youtube/LATEST.md` → develop commit | 運用中 | `docs/10_SNS戦略/06_YouTube運用Playbook.md` |
| `weekly-metrics-issue.yml` | `0 0 * * 1` | 月曜 09:00 JST | `[Weekly Metrics] YYYY-Www` Issue 起票（前週まとめ） | GitHub Issue | 運用中 | #67 |
| `oauth-token-health-check.yml` | `0 0 * * 1` | 月曜 09:00 JST | YouTube OAuth refresh token 生存確認（Production publish で 6 ヶ月有効）、失効時 `[OAuth Alert]` Issue 起票 | GitHub Issue (失効時のみ) | 運用中 | #184 |
| `cloudflare-monthly-snapshot.yml` | `0 0 15 * *` | 毎月 15 日 09:00 JST | 直前の請求サイクル（前月 15 → 当月 14）の使用量集計 + `[Cloudflare Cost Snapshot]` Issue 起票 | `.claude/skills/analytics/cloudflare-cost-improvement/reference/monthly-snapshots/YYYY-MM.json` + Issue | 運用中 | Phase 9 cost |
| `sync-known-keys.yml` | `0 22 * * *` | 毎日 07:00 JST | KNOWN_*_KEYS を remote D1 から再生成、差分があれば PR 自動起票 | `apps/web/src/config/known-{ranking,tag}-keys.ts` | 運用中 | Phase 9 P2-A |
| `improvement-log-reminder-weekly.yml` | `0 13 * * 0` | 日曜 22:00 JST | `docs/05_改善ログ/*.md` の `status: pending|in-progress` かつ deployed_at から 14 日以上経過したエントリを triage、`[Improvement Triage] YYYY-Www` Issue 起票 | GitHub Issue (label: `improvement-log-triage, auto-generated`) | 運用中 | `.claude/scripts/lib/scan-pending-improvements.mjs` |

### 投稿系（schedule trigger）

| workflow | cron (UTC) | 日本時間 | 目的 | ステータス | 関連 |
|---|---|---|---|---|---|
| `post-instagram-scheduled.yml` | `3 0 * * *` | 毎日 09:03 JST | `.claude/state/instagram-w18-schedule.json` 参照、今日と一致するエントリがあれば IG 投稿（IG API 予約非対応の代替） | 運用中 | #127 |

### CI/CD・品質保証系（push trigger）

| workflow | trigger | 目的 | ステータス |
|---|---|---|---|
| `deploy-workers.yml` | push to main | Cloudflare Pages/Workers デプロイ + 施策 ID から improvement-log 自動 stub 追記 | 運用中 |
| `post-deploy-smoke.yml` | deploy 後 | Playwright smoke テスト（商用利用確認） | 運用中 |
| `pr-quality-check.yml` | PR to main | lint / type-check / test / coverage | 運用中 |
| `security-scan.yml` | push to main + 毎週日曜 UTC 00:00 | npm audit + CodeQL | 運用中 |

---

## launchd（ローカル Mac、Mac 起動時のみ動作）

| plist | schedule | 目的 | 呼び出す skill / script | ステータス | 関連 |
|---|---|---|---|---|---|
| `com.stats47.discover-trends` | 毎日 07:23 | トレンド発見バッチ | `npx claude-code -p "/discover-trends --source all"` | 運用中 | - |
| `com.stats47.fetch-note-metrics` | 月曜 09:15 | note.com dashboard から全記事の view/comment/like 取得（Chrome Profile 1 必須） | `.claude/scripts/note/fetch-note-metrics.sh` | 運用中 | #89 |
| ~~`com.stats47.backup-d1`~~ | ~~毎日 03:47~~ | **2026-05-03 廃止**。リモート D1 撤廃 (Phase 10) で対象消失 | — | 停止 | — |
| ~~`com.stats47.weekly-review`~~ | ~~月曜 08:07~~ | **2026-05-16 廃止**。Claude Routine `stats47 weekly PDCA` と完全重複、後者が `/weekly-review` + `/weekly-plan` を一括実行 + PR まで自動化しているため | — | 停止 | — |
| ~~`com.stats47.post-instagram-w18`~~ | ~~毎日 09:03~~ | **2026-04-26 撤去**。GitHub Actions `post-instagram-scheduled.yml` に移行 | — | 停止 | #127 |

### plist の保管場所
- 設定実体: `~/Library/LaunchAgents/com.stats47.*.plist`（gitignore 外、手元のみ）
- wrapper script: `scripts/scheduled/*.sh`（git 管理）
- ログ: `~/Library/Logs/stats47/<name>.log` + `.launchd.out` / `.launchd.err`

### launchd の追加・削除

```bash
# load
launchctl load ~/Library/LaunchAgents/com.stats47.<name>.plist

# unload
launchctl unload ~/Library/LaunchAgents/com.stats47.<name>.plist

# 今すぐ実行（テスト）
launchctl kickstart -k gui/$(id -u)/com.stats47.<name>

# 一覧
launchctl list | grep stats47
```

---

## Claude Routine（Anthropic remote schedule）

リモートで Anthropic サーバが cron fire → stats47 repo に対して claude-code 起動 → PR 作成。**PC 停止中でも動く**のが launchd との最大の違い。

状態保存: `.claude/state/triggers.json`

| id | name | cron (JST) | 用途 | ステータス | 関連 |
|---|---|---|---|---|---|
| `trig_01QBPsFMqaxHdfaSqsjuzCDW` | stats47 weekly PDCA | 土曜 09:03 (3 0 * * 6 UTC) | `/weekly-review` → `/weekly-plan` → develop に PR。**土曜レビュー → 日曜改善 → 月曜実行サイクル** (2026-04-25 から月→土に変更) | 運用中 | #49 (W18 plan) |

### 廃止・統合された Routine 案

| 旧案 | ステータス | 統合先 |
|---|---|---|
| stats47 YouTube weekly review | 統合 → GitHub Actions | `youtube-weekly-review.yml`（月曜 09:15 JST） |
| YouTube Pause Expiry Reminder | 統合 → GitHub Actions | `youtube-audit-daily.yml` 内の `check-pause-events.mjs` ステップ（日次チェック） |
| YouTube Recovery Judgment | 統合 → GitHub Actions | `youtube-audit-daily.yml` 内の `check-pause-events.mjs` ステップ（日次チェック） |

### Routine 管理コマンド

```
# 一覧
RemoteTrigger list

# 停止（無効化）
RemoteTrigger update trigger_id={id} enabled=false

# 削除
RemoteTrigger delete trigger_id={id}
```

---

## 自動化検討中（手動運用が残るもの）

将来自動化したい候補。**各 issue を個別に起票済み**なので、着手する人は対応 issue を開いて受入基準・想定実装・依存関係を確認してから始めること。

ラベル: `automation-candidate` で一覧取得可能（`gh issue list --label automation-candidate`）。

| Issue | 対象タスク | 難度 | 想定実装の概要 |
|---|---|---|---|
| [#285](https://github.com/uruhayato373/stats47/issues/285) | improvement-log の effect/pending 判定リマインダー | S-M | 14 日以上 pending のエントリを週次で検出 → triage Issue 起票 |
| [#286](https://github.com/uruhayato373/stats47/issues/286) | Cloudflare 請求書 PDF 自動取得 + 精算 | L | Gmail MCP + PDF parser、月次 snapshot 予測 vs 実績の突合 |
| [#287](https://github.com/uruhayato373/stats47/issues/287) | auto-generated alert Issue の月初自動 close | S | 14 日以上 stale な auto-generated alert を判別して close |
| [#288](https://github.com/uruhayato373/stats47/issues/288) | `/weekly-plan` の前週残タスク自動転載 | M | Claude Routine 内で `[Weekly Review]` Issue のチェックボックス抽出 |
| [#289](https://github.com/uruhayato373/stats47/issues/289) | Indexing API による問題 URL 自動再送信 | M | `gsc-url-inspection-daily.yml` 拡張、quota 200/day 管理 |

---

## 基盤選定フロー（新規追加時の判断基準）

```
新しい自動化を追加したい
  │
  ├─ ブラウザ自動化 / ローカル資源必須？ (D1 / Chrome login / R2)
  │    YES → launchd
  │    NO ↓
  │
  ├─ AI エージェント（claude-code）に判断させたい？
  │    YES → Claude Routine（PC 停止中でも動く）
  │    NO ↓
  │
  └─ それ以外 → GitHub Actions（最も信頼性高い、PC 非依存）
```

### 具体例
- **PSI 日次計測**: 外部 API 叩くだけ → **GitHub Actions** ✓
- **GSC/GA4 fetch**: SA 鍵で API 叩く → **GitHub Actions** ✓（鍵を Secret に入れる前提）
- **note.com dashboard 取得**: Chrome profile の persistent cookie 必須 → **launchd** ✓
- **/weekly-review 実行**: AI が文章生成 + 判断 → **Claude Routine** ✓（PC 停止中でも動く、PR 作成まで完結）
- **YouTube 週次サマリ**: 既存 CSV から集計するだけ → **GitHub Actions** ✓

---

## 月次ヘルスチェック

毎月初に 3 基盤と docs が一致しているか確認:

```bash
# 1 コマンドで全確認
echo "=== GitHub Actions ===" && gh workflow list --repo uruhayato373/stats47 | grep -v disabled
echo ""
echo "=== launchd ===" && launchctl list | grep stats47
echo ""
echo "=== Claude Routine ===" && cat .claude/state/triggers.json | python3 -m json.tool
echo ""
echo "=== docs ==="
grep -cE "^\| \`com.stats47\.|^\| \`\.github/workflows/|^\| \`trig_" docs/01_技術設計/10_自動化インベントリ.md
```

乖離があれば docs を PR で更新。破損があれば Issue 起票（label: `automation-broken`）。

---

## 関連ファイル

- `scripts/scheduled/_common.sh` — launchd wrapper 共通 env/logger
- `.claude/state/triggers.json` — Claude Routine 状態
- `.claude/state/metrics/` — 各計測の snapshot 置き場
- `.claude/scripts/lib/append-improvement-log.mjs` — deploy 後の improvement-log 自動 stub
- `.claude/scripts/youtube/check-pause-events.mjs` — YouTube pause expiry / recovery judgment
- `.claude/scripts/youtube/youtube-weekly-review.mjs` — YouTube 週次サマリ生成
- `.claude/scripts/sns/sns-weekly-report.mjs` — SNS 週次レポート（data 部）
- `.claude/scripts/cloudflare/monthly-snapshot.mjs` — Cloudflare 月次集計
- `~/Library/LaunchAgents/com.stats47.*.plist` — launchd 実体（手元のみ）
- `~/Library/Logs/stats47/*.log` — 実行ログ（手元のみ）

## 関連 Issue

- #67: GitHub Actions 計測基盤 (Phase 1-3)
- #74: PSI LCP 改善
- #89: note.com メトリクス
- #91: YouTube 運用 Playbook（2026-05-16 に `docs/10_SNS戦略/06_YouTube運用Playbook.md` へ移行し close）
- #127: Instagram Graph API 投稿基盤
