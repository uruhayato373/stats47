# 自動化インベントリ

stats47 の全自動化タスクの静的な真実源。**新規追加・削除時は PR でこのファイルを更新**すること。

## いつ読むか
- どの計測・処理が今動いているか確認したいとき
- 新しい cron を追加する前に既存との重複チェック
- 壊れた自動化を特定するとき（docs を見て `launchctl list` / `gh workflow list` / `RemoteTrigger list` と照合）

## 運用ルール

- **追加時**: PR で本 docs に 1 行追加 + 変更理由を Issue で記録
- **壊れた時**: Issue 起票（label: `automation-broken`）、直ったら close、docs 変更不要（状態は Issue 側）
- **削除時**: PR で docs 行削除 + 関連 Issue に "deprecated" コメント
- **月 1 健康診断**: 月初に 3 基盤と docs が一致しているか確認

## 全実行基盤の概観

| 基盤 | 用途 | 前提 | 停止方法 |
|---|---|---|---|
| GitHub Actions | 公開 / 冪等な処理（PSI / GSC / GA4 / AdSense / Cloudflare cost / Security） | PC 非依存 | workflow 削除 or disable |
| launchd (ローカル Mac) | ブラウザセッション / ローカル資源必須 (D1 / Chrome profile) | Mac 起動必要 | `launchctl unload` |
| Claude Routine (Anthropic remote) | AI エージェント実行（/weekly-review など）、人間判断を自動化 | ANTHROPIC API billing、`.claude/state/triggers.json` | `RemoteTrigger` で `enabled: false` |

---

## GitHub Actions

### 計測・レポート系（schedule trigger）

| workflow | cron (UTC) | 日本時間 | 目的 | 出力先 | ステータス | 関連 |
|---|---|---|---|---|---|---|
| `psi-audit-daily.yml` | `0 17 * * *` | 毎日 02:00 JST | PSI 19 URL × mobile+desktop 計測、閾値違反で `[PSI Alert]` Issue 起票 | `.claude/state/metrics/psi/` → develop commit | 運用中 | #74 |
| `cloudflare-usage-daily.yml` | `30 17 * * *` | 毎日 02:30 JST | Cloudflare GraphQL から R2/Workers/D1 使用量を計測、閾値超過で `[Cloudflare Alert]` Issue 起票 | `.claude/state/metrics/cloudflare/` → develop commit | 運用中 | Phase 9 cost |
| `gsc-url-inspection-daily.yml` | `0 21 * * *` | 毎日 06:00 JST | URL Inspection API で 1,500 URL の Google 認識状態を取得、Coverage Drilldown 集計 | `.claude/state/metrics/gsc/url-inspection/` + `coverage-drilldown/` → main commit + #115 にコメント | 運用中 | #115 |
| `blog-remediation-daily.yml` | `0 23 * * *` | 毎日 08:00 JST | 是正キューを最新化 (公開R2 audit + 最新GSC) して develop commit-back + pending 上位10件を「📝 ブログ是正: 今日のリライト」Issue に upsert。実リライトは人間が Pro セッションで `/brushup-blog --target queue --next 10` (Claude を CI で動かさない・コストゼロ) | `.claude/state/blog/remediation-queue.json` → develop commit + GitHub Issue (label: `auto-generated`) | 運用中 | `.claude/rules/blog-remediation-loop.md` / `.claude/scripts/blog/compose-daily-rewrite-issue.mjs` |
| `fetch-metrics-weekly.yml` | `0 11 * * 0` | 日曜 20:00 JST | GSC/GA4/AdSense 週次 snapshot + NSM 週次 snapshot 生成。**+ 是正キュー再構築 + 是正 wave 効果計測 (measure-gsc-impact, wave_id 駆動) を develop commit** | `.claude/skills/analytics/{gsc,ga4,adsense}-improvement/reference/snapshots/YYYY-Www/` + NSM + `.claude/state/blog/remediation-queue.json` + `improvement-log.md` → develop commit | 運用中 | #67 / `.claude/rules/blog-remediation-loop.md` |
| `sns-metrics-weekly.yml` | `30 11 * * 0` | 日曜 20:30 JST | Instagram メトリクス集計（YouTube は 2026-07 再開したが本 workflow は IG のみ。YT は `/update-sns-metrics` / 診断 workflow で観測） | `.claude/skills/analytics/sns-metrics-improvement/snapshots/YYYY-MM-DD/metrics.csv` → develop commit | 運用中 | #89 |
| `sns-weekly-report.yml` | `0 12 * * 0` | 日曜 21:00 JST | SNS 週次レポート生成（data 部のみ、分析は週次レビュー Routine が追記） | `docs/04_レビュー/YYYY-Www-sns-weekly.md` → develop commit | 運用中 | #89 |
| `weekly-metrics-issue.yml` | `0 0 * * 1` | 月曜 09:00 JST | `[Weekly Metrics] YYYY-Www` Issue 起票（前週まとめ） | GitHub Issue | 運用中 | #67 |
| `agent-consistency-weekly.yml` | `0 22 * * 0` | 月曜 07:00 JST | agent/skill/script 整合性 + 新規未配線checker + git TS↔公開R2 snapshot(page-components/affiliate-ads) + active ranking R2公開漏れ + `.claude/state` サイズ予算を週次検出（`[Consistency Alert]` Issue、自動修正なし） | GitHub Issue | 運用中 | `docs/04_レビュー/2026-07-03-claude-code-setup-audit.md` |
| `instagram-token-refresh.yml` | `0 18 * * 2` | 毎週水 03:00 JST | IGAA 長期トークン（約60日失効）を `ig_refresh_token` で延長し GitHub Secret `INSTAGRAM_ACCESS_TOKEN` を自動更新。IG 投稿/計測の全 workflow がこのトークンに依存 | GitHub Secret 更新 + `.claude/state/metrics/instagram-token.json` | **未稼働（要 `GH_SECRETS_PAT` 登録 + main マージ後に稼働）** | `.claude/scripts/instagram/refresh-token-ci.cjs` |
| `cloudflare-monthly-snapshot.yml` | `0 0 15 * *` | 毎月 15 日 09:00 JST | 直前の請求サイクル（前月 15 → 当月 14）の使用量集計 + `[Cloudflare Cost Snapshot]` Issue 起票 | `.claude/skills/analytics/cloudflare-cost-improvement/reference/monthly-snapshots/YYYY-MM.json` + Issue | 運用中 | Phase 9 cost |
| ~~`sync-known-keys.yml`~~ | — | **廃止** (workflow 不在)。remote D1 撤廃 (完全DBレス) で cron 廃止。KNOWN_*_KEYS は `apps/web/scripts/generate-known-ranking-keys.ts` を **deploy / ranking 公開時**に実行して R2/git TS から再生成 (`ranking-publisher` agent / `/deploy`) | `apps/web/src/config/known-*-keys.ts` | 停止 | — |
| `improvement-log-reminder-weekly.yml` | `0 13 * * 0` | 日曜 22:00 JST | `docs/todo/01_改善バックログ.md` の `status: pending|in-progress` かつ deployed_at から 14 日以上経過したエントリを triage、`[Improvement Triage] YYYY-Www` Issue 起票 | GitHub Issue (label: `improvement-log-triage, auto-generated`) | 運用中 | `.claude/scripts/lib/triage-matrix.mjs` |
| `generate-article-charts.yml` | PR trigger | — | `docs/21_ブログ記事原稿/**/article.md` または `data/*.json` 変更時に SVG 検証 + 結果を PR コメント | PR コメント (slug 別 OK/Failed) | 運用中 | `.claude/scripts/blog/generate-article-charts.ts` |
| `ctr-improvement-monthly.yml` | `0 0 5 * *` | 毎月 5 日 09:00 JST | 最新 GSC snapshot から position 5-15 × CTR < 業界平均 のクエリを抽出、`[CTR Improvement Candidates] YYYY-MM` Issue 起票 | GitHub Issue (label: `ctr-improvement-candidate, auto-generated`) | 運用中 | `.claude/scripts/gsc/extract-low-ctr-queries.mjs` |
| `ksj-catalog-monthly.yml` | `0 18 1 * *` | 毎月 1 日 03:00 JST | 国土数値情報 (jpksj-api) の全データセットカタログを取得、`packages/database/seed/ksj-catalog.json` と diff、追加/削除があれば snapshot を develop に commit + `[KSJ Catalog Update]` Issue 起票 | `packages/database/seed/ksj-catalog.json` → develop commit + GitHub Issue (label: `ksj-catalog, auto-generated`) | 運用中 | `.claude/rules/gis-data.md` |
| `data-refresh.yml` | `0 3 5 * *` | 毎月 5 日 12:00 JST | e-Stat → R2 観測値更新 (完全DBレス・R2 直結)。page-data-batch (e-Stat→`.local/r2/app/stats`) → diff-push-r2 (`app/stats`) → sync-snapshots (派生再生成+push)。旧 db:pull/db:push round-trip は廃止 (db:pull は remotion-static 用 best-effort のみ残置)。schedule 実行=全 metric 実 push、workflow_dispatch=既定 dry_run。**⚠️ 初回 schedule 前に手動 dry_run → 1 metric 実 push で要検証** | R2 `app/stats/*` + 派生 snapshot | 月次 cron (要初回検証) | `docs/01_技術設計/12_完全DBレス設計.md` / `.claude/rules/data-sqlite-ssot.md` |
| `affiliate-ga4-weekly.yml` | `0 13 * * 0` | 日曜 22:00 JST | GA4 から affiliate `ad_impression`/`affiliate_click` を (category×position×variant) 別集計し CTR snapshot 生成 (custom dimension 未登録時は総数に降格)。+ `workflow_dispatch` | `.claude/state/ads/ga4-affiliate-*.json` → develop commit | 運用中 | AFF-05 (`docs/todo/01_改善バックログ.md`) |
| `affiliate-dashboard-refresh.yml` | `0 12 * * 0` | 日曜 21:00 JST | affiliate 在庫の棚卸し JSON + 管理画面 HTML を再生成し commit-back。+ `affiliate-ads-data.ts`/`.claude/scripts/ads/*` 変更時 push トリガ。本番 R2 未変更 | `.claude/state/ads/inventory-*.json` + `docs/40_アフィリエイト管理/affiliate-dashboard.html` → develop commit | 運用中 | AFF-01 |
| `ogp-image-audit-weekly.yml` | `0 18 * * 6` | 日曜 03:00 JST | OGP/リンクカード/note カバーの生成漏れを機械チェックし欠落を**自動修復**(欠落分のみ Satori 生成→R2 push、決定的・LLM 不使用)。修復後も残れば `[OGP Alert]` Issue 起票。inventory を develop commit | `.claude/state/ogp/inventory.json` → develop commit + Issue (label: `ogp-alert, auto-generated`) | 運用中 | `.claude/rules/ogp-image-standards.md` / `/audit-ogp-images` |

### 投稿系（schedule trigger）

| workflow | cron (UTC) | 日本時間 | 目的 | ステータス | 関連 |
|---|---|---|---|---|---|
| `post-instagram-scheduled.yml` | `3 23 * * *` / `3 3 * * *` / `3 10 * * *` | 毎日 08:03 / 12:03 / 19:03 JST | `.claude/state/instagram-w*-schedule.json`（当日を含む週ファイルを自動選択）参照。各実行が「time <= 現在時刻 かつ ig-posted-log 未記録」の最早 1 件を IG 投稿（IG API 予約非対応の代替。1 日複数本対応 2026-07-11〜） | 運用中 | #127 |

### CI/CD・品質保証系（push trigger）

| workflow | trigger | 目的 | ステータス |
|---|---|---|---|
| `deploy-workers.yml` | push to main | Cloudflare Pages/Workers デプロイ + 施策 ID から improvement-log 自動 stub 追記 | 運用中 |
| `blog-auto-publish.yml` | push to develop | ブログ公開ブリッジ。published:true 記事を factual+quality ゲート再検証→R2 公開。上限 10 件/実行。緊急停止は UI の Disable workflow | 運用中 |
| `publish-ai-content.yml` | push (develop, `data/ai-content-staging/*.json`) | 即時 | ranking ai-content の公開ブリッジ (audit gate → R2 push → CDN purge → outbox 掃除)。クラウド生成→git push だけで公開完結 | R2 `app/ranking/<key>/ai-content.json` | 運用中 | `.claude/rules/ranking-content-standards.md` |
| `post-deploy-smoke.yml` | deploy 後 | Playwright smoke テスト（商用利用確認） | 運用中 |
| `pr-quality-check.yml` | PR to main | lint / type-check / test / coverage / build + actionlint + workflow policy契約(permissions/timeout/concurrency/SHA pin) + docs参照の新規broken回帰 + manifest/metadata静的アセット契約 + 画像/SVGポリシー回帰（git管理画像をrepo全体787枚で: デコード・容量・形式↔拡張子・OGP寸法・SVG安全性・SHA-256重複・MD/HTML/CSS/TS(X)参照解決、未参照はwarning） + リポジトリ衛生回帰 + checker配線回帰 + workspace契約 + envレジストリ + route/SEO契約 + 静的accessibility + 代表5 route axe E2E + maintenance debt回帰 + checker実行時間予算/p50/p95 summary + metric registry/catalog生成物鮮度 | 運用中 |
| `security-scan.yml` | push to main + 毎週日曜 UTC 00:00 | npm audit + CodeQL | 運用中 |
| `dependabot.yml` (`github-actions`) | 毎週月曜 04:00 JST | 40桁commit SHAで固定した GitHub Actions の更新PRを生成 | 運用中 |
| `publish-affiliate-ads.yml` | push to develop (`affiliate-ads-data.ts`) + dispatch | git TS SSOT → R2 `app/affiliate-ads/all.json` 反映 + 全ゾーン CDN purge | 運用中 |
| `generate-ogp-images.yml` | dispatch のみ | OGP/カード/note カバーを種別指定で Satori 生成→R2 (手動 fallback。定常は `ogp-image-audit-weekly.yml` + sync-snapshots フック) | 運用中 |
| `sync-snapshots.yml` (sync-ranking-keys job) | dispatch | KNOWN/SITEMAP keys 再生成 + **新規 ranking の OGP/カードを即生成→R2** (公開時フック) | 運用中 |
| `youtube-upload.yml` | push to develop (`.claude/state/youtube-upload-request.json`) + dispatch | YouTube 投稿ブリッジ (量産実験)。リクエストファイルの動画 URL (GitHub Release アセット等) を fetch → CI secrets の OAuth で `upload.js` 実行 (budget/duplicate ガード内蔵) → posts.json commit-back。本番デプロイ不要 | 運用中 (2026-07-11〜) |
| `youtube-shadowban-diagnose.yml` | push to develop (自ファイル) + dispatch | シャドウバン診断 (read-only)。CI secrets の OAuth で `diagnose-shadowban.js` を実行し verdict を artifact/step summary に出力 | 運用中 (2026-07-11〜) |
| `youtube-upload-queue.yml` | `30 8 * * *` (17:30 JST) + dispatch | YouTube 予約仕込みキューの日次消化 (量産実験)。`.claude/state/youtube-upload-queue.json` の pending を 1 日最大 5 本、Release アセット fetch → `upload.js` で private + publish_at 予約アップロード (クォータ 1600units×5 < 10,000/日)。失敗は status=failed で隔離、queue/posts.json を commit-back | 運用中 (2026-07-11〜) |

---

## launchd（ローカル Mac、Mac 起動時のみ動作）

| plist | schedule | 目的 | 呼び出す skill / script | ステータス | 関連 |
|---|---|---|---|---|---|
| `com.stats47.discover-trends` | 毎日 07:23 | トレンド発見バッチ | `npx claude-code -p "/discover-trends --source all"` | 運用中 | - |
| `com.stats47.fetch-note-metrics` | 月曜 09:15 | note.com dashboard から全記事の view/comment/like 取得（Chrome Profile 1 必須） | `.claude/scripts/note/fetch-note-metrics.sh` | 運用中 | #89 |
| ~~`com.stats47.backup-d1`~~ | ~~毎日 03:47~~ | **2026-05-03 廃止**。リモート D1 撤廃 (Phase 10) で対象消失 | — | 停止 | — |
| ~~`com.stats47.weekly-review`~~ | ~~月曜 08:07~~ | **2026-05-16 廃止**。Claude Routine `stats47 weekly PDCA` と完全重複、後者が `/weekly-review` + `/weekly-plan` を一括実行 + PR まで自動化しているため | — | 停止 | — |
| ~~`com.stats47.post-instagram-w18`~~ | ~~毎日 09:03~~ | **2026-04-26 撤去**。GitHub Actions `post-instagram-scheduled.yml` に移行 | — | 停止 | #127 |

### plist の保管場所
- 設定実体: `~/Library/LaunchAgents/com.stats47.*.plist`（gitignore 外、手元のみ）
- wrapper script: `scripts/scheduled/*.sh`（git 管理）
- ログ: `~/Library/Logs/stats47/<name>.log` + `.launchd.out` / `.launchd.err`

### launchd の追加・削除

```bash
# load
launchctl load ~/Library/LaunchAgents/com.stats47.<name>.plist

# unload
launchctl unload ~/Library/LaunchAgents/com.stats47.<name>.plist

# 今すぐ実行（テスト）
launchctl kickstart -k gui/$(id -u)/com.stats47.<name>

# 一覧
launchctl list | grep stats47
```

---

## Claude Routine（Anthropic remote schedule）

リモートで Anthropic サーバが cron fire → stats47 repo に対して claude-code 起動 → PR 作成。**PC 停止中でも動く**のが launchd との最大の違い。

状態保存: `.claude/state/triggers.json`

| id | name | cron (JST) | 用途 | ステータス | 関連 |
|---|---|---|---|---|---|
| `trig_01QBPsFMqaxHdfaSqsjuzCDW` | stats47 weekly PDCA | 土曜 09:03 (3 0 * * 6 UTC) | `/weekly-review` → `/weekly-plan` → develop に PR。**土曜レビュー → 日曜改善 → 月曜実行サイクル** (2026-04-25 から月→土に変更) | 運用中 | #49 (W18 plan) |
| `trig_01RaPLqZrP4i7wAnCzQjifWJ` | stats47 daily trend pipeline | 毎日 09:08 (0 0 * * * UTC) | trend snapshot → `/draft-from-trend` → draft PR 起票。月 5-7 本上限、GATE 1 = 人手レビュー | 運用中 | Phase 3 plan |
| `trig_01J26XQsDbdBM3ggTQ8gcBta` | stats47 weekly CWV PR | 月曜 10:03 (0 1 * * 1 UTC) | 最新 [PSI Alert] Issue → `suggest-cwv-candidates` → LCP 改修案 draft PR 起票。週 1 本、GATE 1 = 人手レビュー | 運用中 | Sprint 5 plan |
| (未登録) | stats47 weekly blog brushup | 週次 (要 `/schedule` 登録) | `select-brushup-candidates` → `fetch-ranking-data-r2` → `brushup-blog --target article` → `quality-gate` → `gh pr create --label brushup-auto`。週 ≤3 本。公開は `blog-auto-publish.yml` が担当 | **計画中** | — |

### 廃止・統合された Routine 案 / GitHub Actions

| 旧案 | ステータス | 統合先 |
|---|---|---|
| ~~stats47 YouTube weekly review~~ | **2026-05-29 YouTube 撤退で廃止** (workflow/script 不在) | — |
| ~~YouTube Pause Expiry Reminder~~ | **2026-05-29 YouTube 撤退で廃止** (workflow/script 不在) | — |
| ~~YouTube Recovery Judgment~~ | **2026-05-29 YouTube 撤退で廃止** (workflow/script 不在) | — |
| `cwv-improvement-pr-weekly.yml` | 廃止 → Routine `stats47 weekly CWV PR` (2026-05-18) | `@anthropic-ai/sdk` + GitHub Secrets `ANTHROPIC_API_KEY` を不要化、Anthropic Pro/Max 内で billing 完結 |

### Routine 管理コマンド

```
# 一覧
RemoteTrigger list

# 停止（無効化）
RemoteTrigger update trigger_id={id} enabled=false

# 削除
RemoteTrigger delete trigger_id={id}
```

---

## 手動運用ツール（オンデマンド実行）

スケジュール化されていない、人間/agent が必要時に呼ぶ on-demand スクリプト。
将来 schedule 化する場合は GitHub Actions / launchd / Claude Routine のいずれかへ移行。

### e-Stat データ管理

| スクリプト | 用途 | 起動例 |
|---|---|---|
| `.claude/scripts/estat/audit-time-coverage.cjs` | e-Stat API で candidate metric の年度カバレッジを実 API 確認 | `node ... audit-time-coverage.cjs` |
| `.claude/scripts/estat/backfill-stats-prefecture.cjs` | D1 stats_prefecture を全年度 upsert (metric 指定 or `--all-priority`) | `node ... --all-priority` |
| `.claude/scripts/estat/restore-from-r2-cache.cjs` | R2 cache → D1 復元 (backfill 事故リカバリ) | `node ... <metric_key>` |

**注意**: `backfill-stats-prefecture.cjs` の現在の実装は **DELETE + INSERT 方式**。
SSDS 以外のソースから来た年度 (例: marriages の 2023/2024) は喪失する。
対策: 実行前に対象 metric の他ソース年度を確認、喪失したら `restore-from-r2-cache.cjs` で復元。
詳細: `docs/todo/01_改善バックログ.md` 参照

### YoY 動画 / Web 用データパイプライン

| スクリプト | 用途 | 起動例 |
|---|---|---|
| `.claude/scripts/web/build-yoy-timeseries.cjs` | 任意 metric の YoY (5年MA) 時系列 JSON 生成、`--batch` で複数同時 | `node ... --batch .claude/config/yoy-batch.json` |
| `.claude/scripts/video/build-population-yoy-data.cjs` | (旧) population 専用版、Remotion video pipeline 用 | `node ...` |

設定: `.claude/config/yoy-batch.json` に 11 metric の出力先マッピング登録済み。
新規 metric を追加するときは batch JSON を編集 → 再実行で完結。

### SNS 投稿 (拡張)

| 既存スキル | 拡張 | 説明 |
|---|---|---|
| `.claude/skills/sns/publish-x/publish-x.ts` | `--media`/`--caption`/`--skip-db` 追加 (2026-05-26) | 任意動画を rankingKey 紐付けなしで X 投稿可能。Remotion 単発作品 (yoy-47等) 用 |
| `.claude/skills/sns/publish-x/publish-x.ts` | `--from-queue [--limit N]` 追加 (2026-07-07) | posts.json の X draft を scheduled_at 昇順で予約消化。頻度ガード + media 再生成 + status=scheduled |

### X 投稿量産システム (2026-07-07 新設)

X 定型投稿を「型 × 画像 × 競合/勝ちパターン」で量産する仕組み。SSOT = `.claude/rules/sns-content-standards.md` §1/§2。

| スクリプト / スキル | 種別 | 説明 |
|---|---|---|
| `.claude/rules/sns-content-standards.md` §2-0/§2-8/§2-9/§1 quota | SSOT (カタログ) | X テンプレ id・角度相性表・画像カタログ・頻度上限を機械参照可能な形で単一ソース化 |
| `.claude/scripts/lib/x-catalog.cjs` | ライブラリ | 上記 md をパース (`getTemplates`/`getAffinity`/`getImageKinds`/`getQuota`)。`--check` 自己検証 |
| `.claude/scripts/lib/svg-to-png.cjs` | ライブラリ | SVG→PNG 共通変換 (note 版から昇格、sharp 失敗で exit≠0) |
| `.claude/scripts/sns/quick-still.ts` (改) | スクリプト | ranking-card 画像を §2-9 正典パスへ出力。`--require-png` で PNG 必須化 |
| `.claude/skills/sns/post-x-batch/` | スキル | 量産本体。候補選定→画像→執筆→lint→draft 登録の 5 フェーズ (owner: x-strategist) |
| `.claude/skills/sns/post-x-batch/scripts/select-candidates.cjs` | スクリプト | 決定的な候補選定 (季節性・dedup・相性でテンプレ割付・scheduled_at 割付) |
| `.claude/skills/sns/post-x-batch/scripts/lint-x-captions.cjs` | スクリプト | 決定的品質ゲート (文字数/タグ/UTM/NG語/類似度) |
| `.claude/skills/sns/post-x-batch/scripts/register-drafts.cjs` | スクリプト | lint PASS を UTM 付き draft として posts.json に登録 |
| `.claude/scripts/sns/check-x-post-budget.cjs` | ガード | §1 quota で日次/週次上限を判定 (YouTube 版の X 相当) |
| `.claude/scripts/sns/promote-scheduled-x.cjs` | スクリプト | 予約時刻経過の scheduled→posted 昇格 |
| `.claude/scripts/sns/backfill-x-templates.cjs` | 1回きり | 既存 X 投稿の template を決定的推定 (`template_inferred` フラグ付き) |
| `.claude/scripts/sns/analyze-x-winning-patterns.mjs` | 分析 | 実測 impressions で勝ち要因抽出 → `.claude/state/sns/x-winning-patterns.json` + docs レビュー |
| `.claude/skills/sns/post-x-6angles/` (削除) | — | §2-8 相性表 + §2-0 template に吸収して廃止 |

### テーマポートフォリオ管理 (2026-07-13 新設・PR-1)

テーマ群 22 件の継続評価・ライフサイクル判定 (keep/merge/retire 等)・実験台帳。運用設計の正典 =
`docs/02_実装計画/25_テーマポートフォリオ運用.md`。owner: `theme-portfolio-manager` agent。
月次/四半期の継続監査コマンドと CI schema 検証の配線は PR-4 で追加予定。

| 対象 | 種別 | 説明 |
|---|---|---|
| `.claude/state/themes/{portfolio,experiments}.json` | state (SSOT) | テーマ別評価 + 実験台帳。ThemeCatalog に変動値を書かない分離設計。schema 正典 = 同 dir README.md |
| `.claude/scripts/themes/validate-theme-state.mjs` | 決定的 lint | schema + 判定規律 enforce (根拠なし merge/retire 禁止・重複実験防止・推測値混入防止・THEME_CATALOGS 整合)。fixture テスト同 dir `__tests__/` |
| `.claude/skills/theme/manage-theme-portfolio/` | スキル | audit/evaluate/experiment/handoff の 4 モード (owner: theme-portfolio-manager) |

---

## 自動化検討中（手動運用が残るもの）

将来自動化したい候補。**各 issue を個別に起票済み**なので、着手する人は対応 issue を開いて受入基準・想定実装・依存関係を確認してから始めること。

ラベル: `automation-candidate` で一覧取得可能（`gh issue list --label automation-candidate`）。

| Issue | 対象タスク | 難度 | 想定実装の概要 |
|---|---|---|---|
| [#285](https://github.com/uruhayato373/stats47/issues/285) | improvement-log の effect/pending 判定リマインダー | S-M | 14 日以上 pending のエントリを週次で検出 → triage Issue 起票 |
| [#286](https://github.com/uruhayato373/stats47/issues/286) | Cloudflare 請求書 PDF 自動取得 + 精算 | L | Gmail MCP + PDF parser、月次 snapshot 予測 vs 実績の突合 |
| [#287](https://github.com/uruhayato373/stats47/issues/287) | auto-generated alert Issue の月初自動 close | S | 14 日以上 stale な auto-generated alert を判別して close |
| [#288](https://github.com/uruhayato373/stats47/issues/288) | `/weekly-plan` の前週残タスク自動転載 | M | Claude Routine 内で `[Weekly Review]` Issue のチェックボックス抽出 |
| [#289](https://github.com/uruhayato373/stats47/issues/289) | Indexing API による問題 URL 自動再送信 | M | `gsc-url-inspection-daily.yml` 拡張、quota 200/day 管理 |

---

## 基盤選定フロー（新規追加時の判断基準）

```
新しい自動化を追加したい
  │
  ├─ ブラウザ自動化 / ローカル資源必須？ (D1 / Chrome login / R2)
  │    YES → launchd
  │    NO ↓
  │
  ├─ AI エージェント（claude-code）に判断させたい？
  │    YES → Claude Routine（PC 停止中でも動く）
  │    NO ↓
  │
  └─ それ以外 → GitHub Actions（最も信頼性高い、PC 非依存）
```

### 具体例
- **PSI 日次計測**: 外部 API 叩くだけ → **GitHub Actions** ✓
- **GSC/GA4 fetch**: SA 鍵で API 叩く → **GitHub Actions** ✓（鍵を Secret に入れる前提）
- **note.com dashboard 取得**: Chrome profile の persistent cookie 必須 → **launchd** ✓
- **/weekly-review 実行**: AI が文章生成 + 判断 → **Claude Routine** ✓（PC 停止中でも動く、PR 作成まで完結）

---

## 月次ヘルスチェック

毎月初に 3 基盤と docs が一致しているか確認:

```bash
# 1 コマンドで全確認
echo "=== GitHub Actions ===" && gh workflow list --repo uruhayato373/stats47 | grep -v disabled
echo ""
echo "=== launchd ===" && launchctl list | grep stats47
echo ""
echo "=== Claude Routine ===" && cat .claude/state/triggers.json | python3 -m json.tool
echo ""
echo "=== docs ==="
grep -cE "^\| \`com.stats47\.|^\| \`\.github/workflows/|^\| \`trig_" docs/01_技術設計/06_自動化インベントリ.md
```

乖離があれば docs を PR で更新。破損があれば Issue 起票（label: `automation-broken`）。

---

## 関連ファイル

- `scripts/scheduled/_common.sh` — launchd wrapper 共通 env/logger
- `.claude/state/triggers.json` — Claude Routine 状態
- `.claude/state/metrics/` — 各計測の snapshot 置き場
- `.claude/scripts/lib/append-improvement-log.mjs` — deploy 後の improvement-log 自動 stub
- `.claude/scripts/sns/sns-weekly-report.mjs` — SNS 週次レポート（data 部）
- `.claude/scripts/cloudflare/monthly-snapshot.mjs` — Cloudflare 月次集計
- `~/Library/LaunchAgents/com.stats47.*.plist` — launchd 実体（手元のみ）
- `~/Library/Logs/stats47/*.log` — 実行ログ（手元のみ）

## 関連 Issue

- #67: GitHub Actions 計測基盤 (Phase 1-3)
- #74: PSI LCP 改善
- #89: note.com メトリクス
- #91: YouTube 運用 Playbook（2026-05-16 に `docs/10_SNS戦略/06_YouTube運用Playbook.md` へ移行し close）
- #127: Instagram Graph API 投稿基盤
