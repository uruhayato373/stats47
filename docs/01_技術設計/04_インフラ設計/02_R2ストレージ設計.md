# R2 ストレージ設計

## 概要

Cloudflare R2 ストレージは、stats47 プロジェクトにおいて静的データの配信とキャッシュ戦略の中核を担います。D1 データベースと連携し、効率的なデータ管理と高速な配信を実現します。

## R2 ストレージの役割

### D1 との使い分け

| 特性               | D1 Database           | R2 Storage               |
| ------------------ | --------------------- | ------------------------ |
| **用途**           | 動的データ、CRUD 操作 | 静的データ、ファイル配信 |
| **クエリ**         | SQL による複雑な検索  | ファイルベースのアクセス |
| **更新**           | リアルタイム          | バッチ更新               |
| **コスト**         | 読み取り課金          | 読み取り無料             |
| **パフォーマンス** | データベースクエリ    | CDN 配信で高速           |

### stats47 での活用方針

- **静的マスタデータ**: 地域情報、カテゴリ情報
- **キャッシュデータ**: API レスポンス、地理データ
- **静的アセット**: ブログ画像、ドキュメント

## R2 バケット構造

```
stats47-r2/
├── area/                     # 地域データ
│   ├── prefectures.json      # 都道府県マスタ
│   └── cities.json           # 市区町村マスタ
├── geoshape/                 # 地理データ
│   ├── prefecture.topojson   # 都道府県データ（全国）
│   └── city/                 # 市区町村データ
│       ├── 01000_merged.topojson  # 北海道（統合版）
│       ├── 01000_split.topojson   # 北海道（分割版）
│       ├── 13000_merged.topojson  # 東京都（統合版）
│       └── ...
├── estat-api/                # e-Stat API キャッシュ
│   ├── meta-info/            # メタ情報キャッシュ
│   │   └── {statsDataId}.json
│   └── stats-data/           # 統計データキャッシュ
│       └── {statsDataId}/
│           └── {params_hash}.json
└── content/                  # 静的コンテンツ
    └── blog/
        └── {slug}/assets/
```

## ドメイン別の R2 活用

### Area ドメイン

**用途**: 地域マスタデータの配信

**ファイル**:

- `area/prefectures.json`: 都道府県マスタ（47 件）
- `area/cities.json`: 市区町村マスタ（1,900+ 件）

**更新**: ビルド時に最新化

**アクセス**: 静的配信（CDN 経由）

**キャッシュ戦略**:

- L1（クライアント）: 1 時間
- L2（CDN）: 1 日
- L3（R2）: 永続（ビルド時更新）

### Geoshape ドメイン

**用途**: TopoJSON データのキャッシュ

**ファイル**:

- `geoshape/prefecture.topojson`: 都道府県データ（全国、低解像度）
- `geoshape/city/{prefCode}_{version}.topojson`: 市区町村データ（都道府県別、高解像度）

**パラメータ**:

- `prefCode`: 都道府県コード（5 桁、例: `01000`、`13000`）
- `version`: `merged`（統合版）または`split`（分割版）

**例**:

- `geoshape/prefecture.topojson`: 都道府県データ（全国）
- `geoshape/city/01000_merged.topojson`: 北海道（統合版）
- `geoshape/city/01000_split.topojson`: 北海道（分割版）
- `geoshape/city/13000_merged.topojson`: 東京都（統合版）

### EstatAPI ドメイン

**用途**: API レスポンスのキャッシュ

**メタ情報キャッシュ**:

- **ファイル**: `estat-api/meta-info/{statsDataId}.json`
- **パラメータ**: `statsDataId`（統計表 ID、8-10 桁）
- **例**: `estat-api/meta-info/0003412313.json`

**統計データキャッシュ**:

- **ファイル**: `estat-api/stats-data/{statsDataId}/{params_hash}.json`
- **パラメータ**:
  - `statsDataId`: 統計表 ID（8-10 桁）
  - `params_hash`: パラメータのハッシュ値（`cdArea`、`cdTime`、`cdCat01`〜`cdCat15`などを含むリクエストパラメータから生成）
- **例**: `estat-api/stats-data/0003412313/a1b2c3d4e5f6g7h8.json`

**TTL**:

- メタ情報: 7 日（手動更新も可能）
- 統計データ: 24 時間（パラメータごとに独立したキャッシュ）
- 統計リスト: 7 日（未実装）

**キャッシュデータ形式**:

- **メタ情報**: オリジナルの API レスポンス（`EstatMetaInfoResponse`）を JSON 形式で保存
- **統計データ**: オリジナルの API レスポンス（`EstatStatsDataResponse`）を JSON 形式で保存

### Content ドメイン

**用途**: ブログ画像などの静的アセット

**ファイル**:

- `content/blog/{slug}/assets/{filename}`

**配信**: CDN 経由の高速配信

**キャッシュ戦略**:

- L2（CDN）: 1 日
- L3（R2）: 永続

## アクセスパターン

### ビルド時アップロード

```typescript
// scripts/upload-to-r2.ts
export async function uploadAreaData() {
  const prefectures = await fs.readFile("data/mock/prefectures.json");
  const cities = await fs.readFile("data/mock/cities.json");

  await r2.putObject("areas/prefectures.json", prefectures);
  await r2.putObject("areas/cities.json", cities);
}
```

### ランタイム読み取り

```typescript
// src/infrastructure/area/repository/AreaRepository.ts
export class AreaRepository {
  async getPrefectures(): Promise<Prefecture[]> {
    const response = await fetch("/api/r2/areas/prefectures.json");
    const data = await response.json();
    return data.prefectures.map(Prefecture.fromJson);
  }
}
```

### キャッシュ更新フロー

```mermaid
graph TD
    A[ビルド時] --> B[R2 にアップロード]
    B --> C[CDN キャッシュ更新]
    C --> D[クライアントアクセス]
    D --> E[CDN から配信]

    F[API レスポンス] --> G[R2 キャッシュ保存]
    G --> H[TTL 管理]
    H --> I[期限切れで削除]
```

## ファイル命名規則

### 地域データ

```
area/
├── prefectures.json          # 都道府県マスタ
└── cities.json               # 市区町村マスタ
```

### 地理データ

```
geoshape/
├── prefecture.topojson       # 都道府県データ（全国、低解像度）
└── city/                     # 市区町村データ
    ├── 01000_merged.topojson # 北海道（統合版、高解像度）
    ├── 01000_split.topojson  # 北海道（分割版、高解像度）
    ├── 13000_merged.topojson # 東京都（統合版、高解像度）
    └── ...
```

### API キャッシュ

```
estat-api/
├── meta-info/                # メタ情報キャッシュ
│   ├── 0003412313.json       # 統計表 ID 別
│   └── 0003000001.json
└── stats-data/               # 統計データキャッシュ
    ├── 0003412313/           # 統計表 ID 別ディレクトリ
    │   ├── {params_hash1}.json
    │   └── {params_hash2}.json
    └── 0003000001/
        └── {params_hash}.json
```

## パフォーマンス最適化

### CDN 配信の活用

- **エッジキャッシュ**: Cloudflare CDN による高速配信
- **圧縮**: gzip 圧縮による転送量削減
- **HTTP/2**: 並列リクエストによる高速化

### キャッシュ戦略

- **階層化キャッシュ**: L1 → L2 → L3 の段階的キャッシュ
- **TTL 最適化**: データ特性に応じた適切な TTL 設定
- **プリフェッチ**: 重要なデータの事前読み込み

### コスト最適化

- **読み取り無料**: R2 の読み取り課金なしを活用
- **圧縮**: ファイルサイズの削減
- **効率的な更新**: 差分更新による転送量削減

## セキュリティ

### アクセス制御

- **公開データ**: CDN 経由で公開
- **プライベートデータ**: 認証が必要なデータは別バケット
- **署名付き URL**: 一時的なアクセス権限

### データ整合性

- **チェックサム**: ファイル整合性の検証
- **バージョン管理**: ファイルのバージョン管理
- **ロールバック**: 問題発生時の復旧機能

## 監視・運用

### メトリクス

- **アクセス頻度**: ファイル別のアクセス統計
- **キャッシュヒット率**: CDN キャッシュの効果測定
- **エラー率**: アクセスエラーの監視

### アラート

- **容量監視**: ストレージ使用量の監視
- **エラー監視**: アクセスエラーの検知
- **パフォーマンス監視**: レスポンス時間の監視

## 関連ドキュメント

- [キャッシュ戦略](./03_キャッシュ戦略.md) - 3 層キャッシュアーキテクチャ
- [ストレージ選択ガイド](./04_ストレージ選択ガイド.md) - D1 vs R2 の判断基準
- [Area ドメイン](01_area.md) - 地域データの管理
- [Geoshape ドメイン](06_geoshape.md) - 地理データの管理

## 更新履歴

- 2025-01-20: 初版作成

# R2 ストレージガイド

## 概要

stats47 プロジェクトにおける Cloudflare R2 ストレージの設計と実装について説明します。

### R2 ストレージとは

Cloudflare R2 は、AWS S3 互換のオブジェクトストレージサービスです。

- **S3 互換 API**: AWS SDK を使用してアクセス可能
- **グローバル CDN**: 世界中で高速アクセス
- **コスト効率**: エグレス料金なし
- **セキュリティ**: エンタープライズグレードのセキュリティ

### D1 との使い分け

| 用途             | D1                  | R2                  |
| ---------------- | ------------------- | ------------------- |
| **データベース** | ✅ 構造化データ     | ❌                  |
| **メタデータ**   | ✅ 検索・フィルタ用 | ✅ 完全なレスポンス |
| **キャッシュ**   | ✅ 高速アクセス     | ✅ 大容量・長期保存 |
| **ファイル**     | ❌                  | ✅ 画像・JSON・CSV  |
| **バックアップ** | ❌                  | ✅ データアーカイブ |

### ユースケース

1. **e-Stat API レスポンスキャッシュ**: 完全な API レスポンスを JSON 形式で保存
2. **統計データアーカイブ**: 過去の統計データを長期保存
3. **画像・ファイル**: ダッシュボード用の画像やエクスポートファイル
4. **バックアップ**: D1 データベースのバックアップファイル

## セットアップ

### R2 バケット作成

#### 環境別バケット構成

```bash
# 開発用バケット
wrangler r2 bucket create stats47-development

# ステージング用バケット
wrangler r2 bucket create stats47-staging

# 本番用バケット
wrangler r2 bucket create stats47
```

#### wrangler.toml の設定

```toml
# デフォルト（本番環境）
[[r2_buckets]]
binding = "METAINFO_BUCKET"
bucket_name = "stats47"
preview_bucket_name = "stats47-development"

# ステージング環境
[env.staging]
NODE_ENV = "production"

[[env.staging.r2_buckets]]
binding = "METAINFO_BUCKET"
bucket_name = "stats47-staging"
```

### アクセス許可の設定

#### Cloudflare R2 トークンの作成

1. **Cloudflare ダッシュボードにログイン**
2. **R2 Object Storage** → **Manage R2 API tokens**
3. **Create API token**をクリック
4. 以下の設定でトークンを作成：
   - **Token name**: `stats47-development`
   - **Permissions**: `Object Read & Write`
   - **Bucket**: `stats47-development`（開発用バケット）

#### 環境変数の設定

**`.env.development`**:

```bash
# R2 S3互換API設定
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_R2_ACCESS_KEY_ID=your_access_key_id
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_secret_access_key
CLOUDFLARE_R2_BUCKET_NAME=stats47-development
```

**本番環境**:

```bash
# Cloudflare Pages/Vercelでの環境変数設定
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_R2_ACCESS_KEY_ID=your_access_key_id
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_secret_access_key
CLOUDFLARE_R2_BUCKET_NAME=stats47
```

### 環境別設定

#### ローカル開発環境

```typescript
// 環境判定による自動切り替え
const getR2Client = () => {
  if (process.env.NODE_ENV === "development") {
    // S3互換APIを使用
    return new S3Client({
      region: "auto",
      endpoint: `https://${process.env.CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com`,
      credentials: {
        accessKeyId: process.env.CLOUDFLARE_R2_ACCESS_KEY_ID!,
        secretAccessKey: process.env.CLOUDFLARE_R2_SECRET_ACCESS_KEY!,
      },
    });
  } else {
    // Cloudflare環境ではR2バインディングを使用
    return env.METAINFO_BUCKET;
  }
};
```

#### Cloudflare 環境

```typescript
// Cloudflare Workers/Pagesでの使用
export async function POST(request: Request, env: Env) {
  const bucket = env.METAINFO_BUCKET; // R2バケットバインディング

  // R2操作
  await bucket.put("key", data);
}
```

## 実装パターン

### R2 への保存実装

#### 基本的な保存パターン

```typescript
export class EstatMetaInfoR2Repository {
  static async saveMetaInfo(
    env: { METAINFO_BUCKET: R2Bucket },
    statsDataId: string,
    metaInfo: EstatMetaInfoResponse
  ): Promise<{ key: string; size: number }> {
    // サマリー情報を抽出
    const tableInf = metaInfo.GET_META_INFO?.METADATA_INF?.TABLE_INF;
    if (!tableInf) {
      throw new Error("統計表情報が見つかりません");
    }

    // R2保存用データ構造に変換
    const r2Data: MetaInfoCacheDataR2 = {
      version: "1.0",
      stats_data_id: statsDataId,
      saved_at: new Date().toISOString(),
      meta_info_response: metaInfo,
      summary: {
        table_title: tableInf.TITLE?.$ || "",
        stat_name: tableInf.STAT_NAME?.$ || "",
        organization: tableInf.GOV_ORG?.$ || "",
        survey_date: tableInf.SURVEY_DATE || "",
        updated_date: tableInf.UPDATED_DATE || "",
      },
    };

    // R2オブジェクトキー生成
    const key = `estat-api/meta-info/${statsDataId}.json`;

    // JSONに変換
    const jsonString = JSON.stringify(r2Data, null, 2);
    const jsonBuffer = new TextEncoder().encode(jsonString);

    // R2に保存
    await env.METAINFO_BUCKET.put(key, jsonBuffer, {
      httpMetadata: {
        contentType: "application/json",
      },
      customMetadata: {
        "stats-data-id": statsDataId,
        "saved-at": r2Data.saved_at,
        "table-title": r2Data.summary.table_title,
      },
    });

    return {
      key,
      size: jsonBuffer.byteLength,
    };
  }
}
```

#### バッチ保存パターン

```typescript
export class BatchR2Repository {
  static async batchSave(
    env: { METAINFO_BUCKET: R2Bucket },
    items: Array<{ key: string; data: any }>
  ): Promise<Array<{ key: string; success: boolean; error?: string }>> {
    const results = await Promise.allSettled(
      items.map(async (item) => {
        const jsonString = JSON.stringify(item.data, null, 2);
        const jsonBuffer = new TextEncoder().encode(jsonString);

        await env.METAINFO_BUCKET.put(item.key, jsonBuffer, {
          httpMetadata: {
            contentType: "application/json",
          },
        });

        return { key: item.key, success: true };
      })
    );

    return results.map((result, index) => {
      if (result.status === "fulfilled") {
        return result.value;
      } else {
        return {
          key: items[index].key,
          success: false,
          error: result.reason.message,
        };
      }
    });
  }
}
```

### R2 からの読み込み

#### 基本的な読み込みパターン

```typescript
export class EstatMetaInfoR2Repository {
  static async getMetaInfo(
    env: { METAINFO_BUCKET: R2Bucket },
    statsDataId: string
  ): Promise<EstatMetaInfoResponse | null> {
    try {
      const key = `estat-api/meta-info/${statsDataId}.json`;
      const object = await env.METAINFO_BUCKET.get(key);

      if (!object) {
        return null;
      }

      const jsonText = await object.text();
      const cacheData: MetaInfoCacheDataR2 = JSON.parse(jsonText);

      return cacheData.meta_info_response as EstatMetaInfoResponse;
    } catch (error) {
      console.error("R2メタ情報キャッシュ取得エラー:", error);
      return null;
    }
  }
}
```

#### 条件付き読み込み

```typescript
export class ConditionalR2Repository {
  static async getWithCondition(
    env: { METAINFO_BUCKET: R2Bucket },
    key: string,
    condition: (metadata: any) => boolean
  ): Promise<any | null> {
    try {
      const object = await env.METAINFO_BUCKET.get(key);

      if (!object) {
        return null;
      }

      // メタデータをチェック
      if (condition(object.customMetadata)) {
        const data = await object.json();
        return data;
      }

      return null;
    } catch (error) {
      console.error("R2条件付き読み込みエラー:", error);
      return null;
    }
  }
}
```

### キャッシュ戦略

#### TTL 付きキャッシュ

```typescript
export class TTLR2Repository {
  static async saveWithTTL(
    env: { METAINFO_BUCKET: R2Bucket },
    key: string,
    data: any,
    ttlHours: number = 24
  ): Promise<void> {
    const expiresAt = new Date();
    expiresAt.setHours(expiresAt.getHours() + ttlHours);

    const cacheData = {
      data,
      expires_at: expiresAt.toISOString(),
    };

    const jsonString = JSON.stringify(cacheData, null, 2);
    const jsonBuffer = new TextEncoder().encode(jsonString);

    await env.METAINFO_BUCKET.put(key, jsonBuffer, {
      httpMetadata: {
        contentType: "application/json",
      },
      customMetadata: {
        "expires-at": expiresAt.toISOString(),
      },
    });
  }

  static async getWithTTL(
    env: { METAINFO_BUCKET: R2Bucket },
    key: string
  ): Promise<any | null> {
    try {
      const object = await env.METAINFO_BUCKET.get(key);

      if (!object) {
        return null;
      }

      const cacheData = await object.json();
      const expiresAt = new Date(cacheData.expires_at);

      if (new Date() > expiresAt) {
        // TTL期限切れ
        await env.METAINFO_BUCKET.delete(key);
        return null;
      }

      return cacheData.data;
    } catch (error) {
      console.error("R2 TTL読み込みエラー:", error);
      return null;
    }
  }
}
```

#### キャッシュヒット・ミス戦略

```typescript
export class CacheStrategyR2Repository {
  static async getWithFallback<T>(
    env: { METAINFO_BUCKET: R2Bucket },
    key: string,
    fallbackFn: () => Promise<T>,
    ttlHours: number = 24
  ): Promise<T> {
    // キャッシュから取得を試行
    const cached = await this.getWithTTL(env, key);

    if (cached) {
      console.log(`R2キャッシュヒット: ${key}`);
      return cached;
    }

    console.log(`R2キャッシュミス: ${key}`);

    // フォールバック関数を実行
    const data = await fallbackFn();

    // キャッシュに保存
    await this.saveWithTTL(env, key, data, ttlHours);

    return data;
  }
}
```

### バッチ処理

#### 大量データの分割処理

```typescript
export class BatchProcessorR2Repository {
  static async processLargeDataset(
    env: { METAINFO_BUCKET: R2Bucket },
    data: any[],
    batchSize: number = 100
  ): Promise<Array<{ key: string; success: boolean; error?: string }>> {
    const results: Array<{ key: string; success: boolean; error?: string }> =
      [];

    for (let i = 0; i < data.length; i += batchSize) {
      const batch = data.slice(i, i + batchSize);

      const batchResults = await Promise.allSettled(
        batch.map(async (item, index) => {
          const key = `batch/${i + index}/${item.id}.json`;
          const jsonString = JSON.stringify(item, null, 2);
          const jsonBuffer = new TextEncoder().encode(jsonString);

          await env.METAINFO_BUCKET.put(key, jsonBuffer, {
            httpMetadata: {
              contentType: "application/json",
            },
          });

          return { key, success: true };
        })
      );

      results.push(
        ...batchResults.map((result, index) => {
          if (result.status === "fulfilled") {
            return result.value;
          } else {
            return {
              key: `batch/${i + index}/${batch[index].id}.json`,
              success: false,
              error: result.reason.message,
            };
          }
        })
      );
    }

    return results;
  }
}
```

## デプロイ

### デプロイ手順

#### 1. ローカルでのテスト

```bash
# 開発環境でテスト（R2保存はスキップされる）
npm run dev:mock

# メタ情報を取得してR2保存ボタンをクリック
# → "開発環境ではR2保存をスキップしました" と表示される
```

#### 2. 本番環境へのデプロイ

```bash
# Cloudflare Workersにデプロイ
wrangler deploy

# または Pagesにデプロイ
wrangler pages deploy
```

#### 3. 本番環境での動作確認

1. 本番環境のアプリケーションにアクセス
2. メタ情報を取得
3. R2 保存ボタンをクリック
4. 成功メッセージとファイルサイズが表示される
5. Cloudflare R2 ダッシュボードでファイルが保存されていることを確認

### 環境別デプロイ

#### ステージング環境

```bash
# ステージング環境にデプロイ
wrangler deploy --env staging

# ステージング環境でのテスト
curl -X POST https://staging.stats47.com/api/estat-api/metainfo-cache/save \
  -H "Content-Type: application/json" \
  -d '{"statsDataId": "0000010101", "metaInfoResponse": {...}}'
```

#### 本番環境

```bash
# 本番環境にデプロイ
wrangler deploy

# 本番環境でのテスト
curl -X POST https://stats47.com/api/estat-api/metainfo-cache/save \
  -H "Content-Type: application/json" \
  -d '{"statsDataId": "0000010101", "metaInfoResponse": {...}}'
```

### トラブルシューティング

#### よくある問題

1. **"R2 バケットが設定されていません"エラー**

   - 原因: 環境変数`METAINFO_BUCKET`が設定されていない
   - 解決: Cloudflare 環境で R2 バケットの環境変数を設定

2. **権限エラー**

   - 原因: R2 バケットへの書き込み権限がない
   - 解決: Cloudflare アカウントで R2 バケットの権限を確認

3. **開発環境で R2 保存されない**
   - 原因: 正常な動作（開発環境ではスキップされる）
   - 解決: 本番環境でテストする

## ベストプラクティス

### オブジェクトキーの命名規則

#### 階層構造の設計

```
{domain}/{category}/{id}/{filename}
```

**例**:

```
estat-api/meta-info/0000010101.json
estat-api/stats-data/0000010101/a1b2c3d4e5f6g7h8.json
geoshape/city/01000_merged.topojson
area/prefectures.json
exports/ranking/2025-01-13/ranking.csv
images/dashboard/charts/chart-001.png
```

#### バージョニング

```
{domain}/{category}/{id}/v{version}/{filename}
```

**例**:

```
estat-api/meta-info/0000010101.v1.json
estat-api/meta-info/0000010101.v2.json
```

### メタデータの活用

#### カスタムメタデータの設定

```typescript
await bucket.put(key, data, {
  customMetadata: {
    "stats-data-id": statsDataId,
    "saved-at": new Date().toISOString(),
    "table-title": tableTitle,
    version: "1.0",
    "content-type": "application/json",
  },
});
```

#### メタデータベースの検索

```typescript
export class MetadataSearchR2Repository {
  static async searchByMetadata(
    env: { METAINFO_BUCKET: R2Bucket },
    filters: Record<string, string>
  ): Promise<Array<{ key: string; metadata: any }>> {
    const results: Array<{ key: string; metadata: any }> = [];

    // プレフィックスで絞り込み
    const prefix = filters.prefix || "";
    const list = await env.METAINFO_BUCKET.list({ prefix });

    for (const object of list.objects) {
      const metadata = object.customMetadata || {};

      // フィルタ条件をチェック
      const matches = Object.entries(filters).every(([key, value]) => {
        if (key === "prefix") return true;
        return metadata[key] === value;
      });

      if (matches) {
        results.push({
          key: object.key,
          metadata,
        });
      }
    }

    return results;
  }
}
```

### パフォーマンス最適化

#### 並列処理

```typescript
export class ParallelR2Repository {
  static async parallelGet(
    env: { METAINFO_BUCKET: R2Bucket },
    keys: string[]
  ): Promise<Array<{ key: string; data: any | null; error?: string }>> {
    const results = await Promise.allSettled(
      keys.map(async (key) => {
        try {
          const object = await env.METAINFO_BUCKET.get(key);
          const data = object ? await object.json() : null;
          return { key, data };
        } catch (error) {
          return { key, data: null, error: error.message };
        }
      })
    );

    return results.map((result, index) => {
      if (result.status === "fulfilled") {
        return result.value;
      } else {
        return {
          key: keys[index],
          data: null,
          error: result.reason.message,
        };
      }
    });
  }
}
```

#### ストリーミング処理

```typescript
export class StreamingR2Repository {
  static async streamLargeFile(
    env: { METAINFO_BUCKET: R2Bucket },
    key: string,
    chunkSize: number = 1024 * 1024 // 1MB
  ): Promise<ReadableStream> {
    const object = await env.METAINFO_BUCKET.get(key);

    if (!object) {
      throw new Error(`Object not found: ${key}`);
    }

    const body = object.body;
    if (!body) {
      throw new Error(`Object body is null: ${key}`);
    }

    return body;
  }
}
```

### コスト最適化

#### ライフサイクル管理

```typescript
export class LifecycleR2Repository {
  static async setLifecyclePolicy(
    env: { METAINFO_BUCKET: R2Bucket },
    key: string,
    daysToExpire: number
  ): Promise<void> {
    const expiresAt = new Date();
    expiresAt.setDate(expiresAt.getDate() + daysToExpire);

    await env.METAINFO_BUCKET.put(key, data, {
      customMetadata: {
        "expires-at": expiresAt.toISOString(),
      },
    });
  }

  static async cleanupExpired(
    env: { METAINFO_BUCKET: R2Bucket },
    prefix: string = ""
  ): Promise<number> {
    const list = await env.METAINFO_BUCKET.list({ prefix });
    const now = new Date();
    let deletedCount = 0;

    for (const object of list.objects) {
      const expiresAt = object.customMetadata?.["expires-at"];

      if (expiresAt && new Date(expiresAt) < now) {
        await env.METAINFO_BUCKET.delete(object.key);
        deletedCount++;
      }
    }

    return deletedCount;
  }
}
```

#### 圧縮の活用

```typescript
export class CompressionR2Repository {
  static async saveCompressed(
    env: { METAINFO_BUCKET: R2Bucket },
    key: string,
    data: any
  ): Promise<{ key: string; size: number; compressedSize: number }> {
    const jsonString = JSON.stringify(data, null, 2);
    const originalSize = new TextEncoder().encode(jsonString).byteLength;

    // Gzip圧縮
    const compressed = await this.compress(jsonString);

    await env.METAINFO_BUCKET.put(key, compressed, {
      httpMetadata: {
        contentType: "application/json",
        contentEncoding: "gzip",
      },
    });

    return {
      key,
      size: originalSize,
      compressedSize: compressed.byteLength,
    };
  }

  private static async compress(text: string): Promise<Uint8Array> {
    const stream = new CompressionStream("gzip");
    const writer = stream.writable.getWriter();
    const reader = stream.readable.getReader();

    writer.write(new TextEncoder().encode(text));
    writer.close();

    const chunks: Uint8Array[] = [];
    let done = false;

    while (!done) {
      const { value, done: readerDone } = await reader.read();
      done = readerDone;
      if (value) {
        chunks.push(value);
      }
    }

    const totalLength = chunks.reduce((sum, chunk) => sum + chunk.length, 0);
    const result = new Uint8Array(totalLength);
    let offset = 0;

    for (const chunk of chunks) {
      result.set(chunk, offset);
      offset += chunk.length;
    }

    return result;
  }
}
```

## 関連ドキュメント

- [D1 データベース概要](01_D1データベース概要.md) - D1 データベースの設計とアーキテクチャ
- [D1 実装ガイド](02_D1実装ガイド.md) - D1 データベースの開発・実装手順
- [環境設定ガイド](04_環境設定ガイド.md) - 環境固有の設定
- [運用・保守ガイド](05_運用・保守ガイド.md) - 運用保守の手順
