# 週次メトリクス 2026-W14（2026-03-30〜04-05）

## GA4

未取得（`/fetch-ga4-data` で取得予定）

## GSC

未取得（`/fetch-gsc-data` で取得予定）

## YouTube

未取得（`/fetch-youtube-data` で取得予定）

## SNS パフォーマンス

| プラットフォーム | フォロワー | 今週投稿数 | 主要指標 |
|---|---|---|---|
| X | - | - | インプレッション: - |
| Instagram | - | - | リーチ: - |
| TikTok | - | - | 再生数: - |
| note | - | - | PV: - |

※ `/update-sns-metrics` 実行後に D1 `sns_metrics` テーブルへ自動蓄積される。記入時は以下のクエリで取得:
```sql
SELECT p.platform, COUNT(DISTINCT m.sns_post_id) as posts,
       SUM(m.impressions) as impressions, SUM(m.views) as views, SUM(m.likes) as likes
FROM sns_metrics m JOIN sns_posts p ON m.sns_post_id = p.id
WHERE m.fetched_at = (SELECT MAX(fetched_at) FROM sns_metrics)
GROUP BY p.platform;
```

## AdSense

- 推定収益（週次）: 未取得

## アフィリエイト

- クリック数: 未取得
- 成約数: -
- 収益: -

## 前週（W13）比較

| 指標 | W13 | W14 | 増減 |
|---|---|---|---|
| GA4 PV（28日） | 3,518 | - | - |
| GSC クリック（28日） | 98 | - | - |
| GSC 平均順位 | 15.6 | - | - |

## 所感

（記入予定）
