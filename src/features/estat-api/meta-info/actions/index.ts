/**
 * e-Statメタ情報関連のサーバーアクション
 *
 * 注意: Server Actionsは直接importすることを推奨します。
 * バレルエクスポート（再エクスポート）は避けてください（manifest解決が不安定になるため）。
 *
 * @example
 * ```tsx
 * // 推奨: 直接import
 * import { saveMetaInfoAction } from "@/features/estat-api/meta-info/actions/saveMetaInfoAction";
 *
 * // 非推奨: バレルエクスポート経由
 * import { saveMetaInfoAction } from "@/features/estat-api/meta-info/actions";
 * ```
 */

export { saveMetaInfoAction } from "./saveMetaInfoAction";

