/**
 * 手置き AdSense 枠を「どのビューポートで出すか」の単一ソース。
 *
 * ## 決定 (2026-08-04・W31 実測)
 *
 * **モバイルでは手置き枠を出さず、Auto ads に任せる。**
 *
 * AdSense placement 別の実測 (2026-07-26〜08-01):
 *
 * | placement          | 収益 | imp   | RPM  | viewability | requests | 充足  |
 * |--------------------|-----:|------:|-----:|------------:|---------:|------:|
 * | AUTO_ADS  Mobile   |  ¥33 |   287 | ¥114 |       74.0% |    1,207 | 80.5% |
 * | MANUAL    Mobile   |   ¥4 |   495 |   ¥8 |       28.6% |    2,037 | 25.7% |
 * | AUTO_ADS  Desktop  |  ¥73 |   681 | ¥107 |       86.3% |    2,734 | 60.0% |
 * | MANUAL    Desktop  |  ¥34 | 2,640 |  ¥13 |       48.9% |    9,333 | 28.9% |
 *
 * モバイルの手置き枠は **RPM で Auto ads の 1/14**、リクエストの 4 分の 3 が空振りし、
 * 表示できた分も **7 割が視認されていない**。週 ¥4 (全収益の 2.7%) と引き換えに
 * 2,037 リクエストと 495 の描画を消費している。
 *
 * 実ユーザーの LCP は p75 = 2,476ms で閾値 2,500ms まで 24ms しか余裕がなく、
 * モバイルが最も苦しい。**見られない広告は収益を生まないのに CWV と読者体験だけ削る**ため、
 * 収益レーンの入れ替えではなく不良在庫の整理として撤去する。
 * 収益化戦略 SSOT §3.1「低 viewability / 低 imp RPM の枠は、追加ではなく
 * 移設・形式変更・撤去を比較する」に沿う。
 *
 * ## デスクトップは残す
 *
 * デスクトップの手置き枠は週 ¥34 (23%) あるため同じ理屈では切れない。
 * 枠単位の内訳 (unit_id ↔ コード側 slot_id) が要るが、その ad-units inventory は
 * 2026-08-04 まで一度も取得に成功していなかった (`fetch-adsense-snapshot.mjs` を同日修正)。
 * inventory が入ってから個別に判断する。
 *
 * ## 未検証の前提
 *
 * 手置き枠を外した領域に Auto ads がどう反応するかは**測っていない**。
 * 増える (空きに置ける) 可能性も、変わらない可能性もある。確実なのは
 * 「週 ¥4 を失う」ことだけで、それ以外は撤去後の実測を待つ。
 *
 * ## 実装
 *
 * `display:none` の要素は IntersectionObserver が交差を報告しないため、
 * `AdSenseAd` の遅延ロードが発火せず **広告リクエスト自体が飛ばない**。
 * クライアント側の viewport 判定を使わないので SSR と hydration がずれない。
 */

/**
 * 手置き枠のコンテナに付けるクラス。モバイル (< 768px) では描画しない。
 *
 * 境界は `AdSenseAd` の `MOBILE_MEDIA_QUERY` (`max-width: 767px`) と一致させている。
 * 片方だけ動かすと「モバイル判定では mobile なのに枠は出ている」状態になるため、
 * 変更するときは両方を同時に見ること。
 */
export const MANUAL_AD_DESKTOP_ONLY_CLASS = "hidden md:block";
