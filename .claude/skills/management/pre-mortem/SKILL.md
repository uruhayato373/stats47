---
name: pre-mortem
description: プロジェクトの Pre-Mortem（事前検死）を実施。1年後に完全に頓挫した想定で、具体的な失敗シナリオと対策書を現在のデータから生成する。
disable-model-invocation: true
primary_agent: strategy-advisor
---

プロジェクトの **Pre-Mortem（事前検死）** を実施する。
「1年後、このプロジェクトは完全に失敗した。何が起きたか？」を、現在の実態データに基づいて具体的に描写し、各シナリオに対する **予防策・耐性策** を設計する。

失敗シナリオはセッション内で分析し、**採択した未完了の予防策だけをTODOへ統合する**。

## 目的

- 楽観バイアスを打ち破り、見て見ぬふりしているリスクを言語化する
- 「いつかやる」と先送りしている致命的タスクを炙り出す
- 定量データで裏付けた失敗シナリオにより、優先順位の再設定を促す
- 各リスクに対して「予防」と「耐性」の2層で備える

## 手順

### Phase 1: 現在の実態データを収集

以下を**すべて実際のソースから直接取得**する。計画書やロードマップの記載は信用しない。

#### コンテンツ規模（完全DBレス: R2 snapshot / git TS から。旧 D1/miniflare は廃止）

```bash
R2="https://storage.stats47.jp"
# ランキング数（= 旧 metrics / metrics_active / 観測値を持つ metric 数の代理。公開 active 集合）
echo "rankings(active): $(curl -s "$R2/app/ranking-items/all.json" | jq '.count')"
# 公開記事数（export-blog-snapshot は published のみ出力）
echo "articles:         $(curl -s "$R2/app/blog/all.json" | jq '.articles | length')"
# 相関ペア総数
echo "correlations:     $(curl -s "$R2/app/correlation/stats.json" | jq '.total')"
# SNS 投稿件数は投稿台帳 posts.json から:
node -e 'const s=require("./.claude/scripts/lib/sns-posts-store.cjs");console.log("sns_posts:",s.loadAll().length)'
# area_profiles は Derived（エフェメラル計算 → R2 app/areas/<code>/profile.json、47 都道府県分）
# 全 metric 定義（inactive 含む）を数えるなら git TS: ls packages/data-configs/src/metrics/*.ts | wc -l
```

#### コンテンツ資産

- `.local/r2/blog/` の記事数と公開状態（publishedAt の有無）
- `.local/r2/sns/` の未投稿コンテンツ数
- `apps/remotion/` のテンプレート種類数

#### 外部指標（取得可能な場合）

- Google Search Console: 直近30日のインプレッション・クリック数（`/fetch-gsc-data` が使える場合）
- GA4: 直近30日の PV・ユーザー数（`/fetch-ga4-data` が使える場合）
- SNS: フォロワー数・投稿数（各 `/fetch-*-data` スキル）

外部 API にアクセスできない場合はスキップし、その旨を明記する。

#### Git 活動

```bash
git log --oneline --since="30 days ago" | wc -l   # 直近30日のコミット数
git log --oneline --since="7 days ago" | wc -l     # 直近7日のコミット数
```

#### インフラ

- Cloudflare D1 の行数（ranking_data が free tier 上限に近いか）
- R2 バケットのサイズ（`/r2-du` の直近結果があれば参照）
- `.git/` のサイズ

#### 既存の対策状況

- `apps/web/src/middleware.ts` のリダイレクト設定
- `apps/web/src/app/sitemap.ts` の動的生成状況
- AdSense / アフィリエイトの設定状況
- 実際に稼働している仕組み（自動投稿パイプライン等）の有無

### Phase 2: 失敗シナリオを生成（前半セクション）

収集したデータをもとに、**1年後に完全に頓挫した**前提で失敗シナリオを 7〜10 個書く。これはファイル本文の「## 想定失敗シナリオ」セクションに入れる。

#### シナリオの書き方ルール

1. **見出し**: 失敗の本質を1行で（例: 「配信恐怖症が最後まで治らなかった」）
2. **冒頭**: 現在のデータから、なぜこの失敗が起きうるかを1〜2文で
3. **「具体的に何が起きたか」**: 箇条書き3〜5項目。**過去形**で書く（1年後の視点）
4. **数字を入れる**: 「約」ではなく具体的な数字（現在のデータから外挿）
5. **発生確率・影響度**: 高 / 中 / 低 のいずれかを明記
6. **予兆サイン**: 何を見たら兆候と判断するか

#### 必須カテゴリ（該当する場合）

- **配信・公開の停滞**: 作ったが届けていないコンテンツ
- **SEO・検索流入の崩壊**: インデックス状況、CTR低下リスク
- **収益ゼロの長期化**: AdSense、アフィリエイト、有料記事の状況
- **技術的楽しさへの逃避**: 手段の目的化、過剰な自動化
- **インフラ・技術的負債**: D1上限、SSD容量、依存パッケージ
- **一人プロジェクトの限界**: 稼働時間、モチベーション管理
- **外部環境の変化**: API仕様変更、AI Overview、競合動向
- **データ品質**: AI生成コンテンツの誤り、データ鮮度

### Phase 3: 対策を生成（後半セクション）

Phase 2 の各シナリオに対して、**予防策（Prevention）** と **耐性策（Resilience）** の2層で対策を設計する。これはファイル本文の「## 対策」セクションに入れる。

#### 対策の書き方ルール

1. **対応**: シナリオ番号と連動させる（`シナリオ 1 → 対策 1`）
2. **予防策**: そもそも起きないようにする仕組み。具体的な手順・最小実装を含む。チェックボックスで列挙
3. **耐性策**: 起きても致命傷にならない構造。「なぜ大丈夫か」を明示。チェックボックスで列挙
4. **既に対策済みの場合**: 調査結果を明記し、追加で必要な監視・改善を提案
5. **具体的な判断基準を含める**: 「X ヶ月後に Y 未満なら Z する」の形式

### Phase 3.5: ロードマップの Sprint 進捗評価

`docs/02_実装計画/00_INDEX.md`、`docs/00_プロジェクト管理/02_収益化戦略.md`、
`docs/00_プロジェクト管理/03_マーケティング戦略.md` を読み込み、四半期の区切りとして以下を評価する:

1. **「現在のステータス」テーブル**: Phase 1 で取得した実測値で更新（`/weekly-review` と同じ手順）
2. **Sprint 完了判定**: 現在の Sprint の「完了条件」を実績データと照合。達成/未達を明記
3. **次 Sprint への示唆**: 対策書の提言を踏まえ、次 Sprint で優先すべき事項を提案（ただし Sprint 定義そのものは書き換えない。提案としてファイル末尾に「次 Sprint 提案」サブセクションで記載する）

**変更してよい範囲**: 「現在のステータス」テーブルの数値・状態のみ。「基本方針」「Sprint 定義」「凍結タスク」は変更しない。

### Phase 4: 対策の統合

1. 採択した予防策を、改善なら `.claude/todo/04_改善バックログ.md`、機能・自動化なら
   `05_機能バックログ.md` へ統合する。
2. 各項目に優先度、trigger、最初の実行手順、停止条件、完了条件を付ける。
3. 恒久的な禁止事項や判断基準は既存の戦略・rulesへ直接反映する。
4. 失敗シナリオ全文は保存せず、セッションの最終報告で提示する。

### Phase 5: 報告

追加・更新したTODO IDと、最も優先度の高い対策3つを報告する。

## ファイル本文フォーマット

```markdown
## 対象期間 / プロジェクト
- **期間**: YYYY-MM-DD 〜 YYYY-MM-DD
- **作成日**: YYYY-MM-DD
- **想定時点**: {1年後}年{同月}月。プロジェクト開始から{N}年。完全に頓挫。
- **対象**: stats47 プロジェクト全体

## 想定失敗シナリオ

### シナリオ 1: {失敗の本質を1行で}

{現在のデータに基づく導入（1〜2文）}

- **具体的に何が起きたか**:
  - ...（過去形で箇条書き 3〜5 項目、具体的な数字を含む）
  - ...
- **発生確率**: 高 / 中 / 低
- **影響度**: 高 / 中 / 低
- **予兆サイン**: ...

### シナリオ 2: {タイトル}

<!-- 同じ構造で 7〜10 シナリオ -->

## 失敗の根本原因（1文で）

**{1文}**

{補足2〜3文}

## 対策

対策は2層で設計する:
- **予防策（Prevention）**: そもそも起きないようにする
- **耐性策（Resilience）**: 起きても致命傷にならないようにする

### 対策 1: シナリオ 1 → {対策の要約}

#### 予防策: {具体的な仕組み}
{説明}
- [ ] 最小実装ステップ 1
- [ ] 最小実装ステップ 2

#### 耐性策: {なぜ大丈夫か}
- [ ] ...
- [ ] ...

### 対策 2: シナリオ 2 → ...

<!-- 以降、シナリオと同数 -->

## まとめ: 最悪のシナリオでも「大丈夫」な理由

| 最悪のケース | なぜ大丈夫か |
|---|---|
| ... | ... |

**{プロジェクトが本当に死ぬ唯一のパターンを1文で}**

## 次 Sprint 提案

<!-- Phase 3.5 の成果。ロードマップ本体は書き換えず、提案のみ -->

## 検証タイミング

- **次回再評価**: YYYY-MM-DD
- **実測値が出る時期**: YYYY-MM-DD

## 関連ドキュメント

<!-- 関連 critical-review / 週次レビュー（相対リンク）、残存 enhancement Issue は #番号で -->
```

## トーンと姿勢

### 失敗シナリオ（前半セクション）
- **容赦なく、しかし建設的に**: 問題を美化しない。「まだ大丈夫」は禁句
- **過去形で書く**: 「〜だろう」ではなく「〜だった」「〜してしまった」
- **数字で語る**: 「たくさん」ではなく「146件」「0件」「82%」
- **自分事として書く**: 第三者の分析ではなく、失敗した本人の回顧として

### 対策（後半セクション）
- **現在形・命令形で書く**: 「〜すべきだった」ではなく「〜する」
- **最小実装を示す**: 理想論ではなく、今週中にできる最小ステップ
- **判断基準を明確に**: 「いつ」「何を見て」「どう判断するか」を具体的に
- **既に対策済みのものは正当に評価する**: 無理にリスクを誇張しない

### 共通
- 既存対策との重複は `.claude/todo/` と対象rulesを検索して防ぐ。

## 保存先

未完了策は `.claude/todo/`、恒久判断は既存SSOT。分析全文は保存しない。

## 参照

- `git log -- .claude/todo/` — 過去に採択・完了した対策
- `docs/02_実装計画/00_INDEX.md` — 実装計画の現在地
- `docs/00_プロジェクト管理/02_収益化戦略.md` — 収益レーン・意思決定ゲート
- `docs/00_プロジェクト管理/03_マーケティング戦略.md` — T1〜T4・PVモデル
- `.claude/skills/management/critical-review/SKILL.md` — 批判的レビュー（計画書向け、対になるスキル）
