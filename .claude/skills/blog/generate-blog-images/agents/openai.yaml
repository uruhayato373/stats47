interface:
  display_name: "Generate Blog Images"
  short_description: "Generate reproducible stats47 blog images"
  default_prompt: "Use $generate-blog-images to generate and validate a blog thumbnail background."
policy:
  allow_implicit_invocation: false
