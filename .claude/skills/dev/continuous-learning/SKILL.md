---
name: continuous-learning
description: セッション中の再利用可能なパターンを抽出し learned/ に保存する。Use when user says "パターン記録", "continuous-learning", "学び保存". 同じエラーを2回解決した場合に自動適用.
---

セッション中に発見した再利用可能なパターン（エラー解決策・ワークアラウンド・ユーザー修正）を抽出し、`.claude/skills/learned/` に保存する。

## 用途

- バグ修正完了後にパターンを記録（`/knowledge` の自動版）
- ユーザーから「そうじゃない」「こうして」と修正を受けた後
- 同じエラーを 2 回以上解決した場合
- フレームワーク固有のワークアラウンドを発見した場合
- CLAUDE.md「行動原則 3. ミスから学ぶ」に該当する状況を検出した場合

## `/knowledge` との違い

| 項目 | `/knowledge` | `/continuous-learning` |
|---|---|---|
| 記録内容 | 何が起きたか（問題・原因・対策） | どう対処するか（トリガー → アクション） |
| 記録タイミング | バグ解決時に手動実行 | セッション中に随時 |
| 保存先 | `.claude/skills/management/knowledge/SKILL.md` 末尾 | `.claude/skills/learned/{pattern-name}.md` 個別ファイル |
| 活用方法 | 過去の失敗を参照 | 同じ状況で自動的に対処パターンを適用 |

## 手順

### Phase 1: パターン検出

以下の 5 カテゴリからパターンを探す:

1. **エラー解決**: 今回のセッションで解決したエラーとその手順
2. **ユーザー修正**: ユーザーが「こうして」と指示した非自明な方法
3. **ワークアラウンド**: フレームワーク/ライブラリの制限を回避した方法
4. **デバッグ手法**: 問題の特定に有効だったデバッグアプローチ
5. **プロジェクト規約**: コードベース固有の暗黙ルール

### Phase 2: パターン記録

`.claude/skills/learned/{pattern-name}.md` に以下の形式で保存:

```markdown
# {パターン名}

**トリガー**: どんな状況で発動するか（具体的なエラーメッセージ、ファイルパターン等）
**対処**: 具体的なアクション（コマンド、コード変更等）
**根拠**: なぜこの対処が有効か
**確信度**: 0.3（初回）〜 0.9（複数回確認済み）
**発見日**: YYYY-MM-DD
**関連**: 関連するファイルパス、スキル、ドキュメント
```

### Phase 3: 既存パターンの更新

同じトリガーのパターンが既に存在する場合:
- 確信度を +0.1 増加（上限 0.9）
- 新しい根拠やコンテキストを追記
- 発見日を最新に更新

## 注意

- CLAUDE.md や既存ドキュメントに既に記載されている内容は記録しない
- コードパターン・アーキテクチャ（コードを読めば分かること）は記録しない
- 一時的な状態（デバッグ中の作業メモ等）は記録しない
- パターンファイルが 20 個を超えたら、確信度 0.3 以下のものを整理する

## 関連スキル

- `/knowledge` — 過去の失敗と学びの手動記録
